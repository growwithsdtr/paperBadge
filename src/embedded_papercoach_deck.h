#pragma once

#include <Arduino.h>

namespace embedded_papercoach {

struct Card {
  const char* id;
  const char* sectionId;
  const char* section;
  const char* number;
  const char* title;
  bool mustMaster;
  const char* theme;
  const char* spoken;
  const char* anchor;
  const char* confidence;
  const char* watch;
};

struct Drill {
  const char* id;
  const char* type;
  const char* cardId;
  const char* prompt;
  const char* options[4];
  uint8_t optionCount;
  uint8_t correctIndex;
  const char* explanation;
};

constexpr size_t kCardCount = 71;
constexpr size_t kMustMasterCount = 17;
constexpr size_t kDrillCount = 149;
constexpr size_t kSourceJsonBytes = 199783;
constexpr size_t kDrillsJsonBytes = 80353;
constexpr const char* kSourcePath = "sample-data/papercoach/interview_prep_sheet3.md";

const Card kCards[] PROGMEM = {
  {
    R"PB0(A01)PB0",
    R"PB0(A)PB0",
    R"PB0(Background, Motivation & Fit)PB0",
    R"PB0(1)PB0",
    R"PB0(Self-introduction / career & recent work)PB0",
    true,
    R"PB0(Background/Fit)PB0",
    R"PB0(I'm a senior technical PM who pairs a research background in AI with about 15 years of product and operating experience, and my lane is taking AI from prototype into production in commercially sensitive workflows. Three things define me: product judgment in discovery — deciding what's worth building and whether it monetizes; enough technical depth to scope cost, latency, and feasibility with engineers instead of guessing; and a zero-to-one record, from building a profitable D2C brand to diagnosing and turning around an AI product on engagement. My most production-grade AI work has been enterprise RAG pilots, and my most recent hands-on build won the AI Voice Agents Hackathon Tokyo 2026 — so I still ship, not just advise.)PB0",
    R"PB0(RAG pilots (Digital Senpai) as the production-grade signal; hackathon as recency; Pita (250% ROI Yr2) and Cotomo (~3× retention) as range. Alt: lead with Safed COO for operator-heavy roles.)PB0",
    R"PB0(Evidence-backed.)PB0",
    R"PB0(Over-talk risk — three strengths, one recent signal, stop. Have your exact PhD status ready so "research background" can't read as evasive.)PB0",
  },
  {
    R"PB0(A02)PB0",
    R"PB0(A)PB0",
    R"PB0(Background, Motivation & Fit)PB0",
    R"PB0(2)PB0",
    R"PB0(Why leave consulting for an in-house role)PB0",
    true,
    R"PB0(Motivation)PB0",
    R"PB0(The honest reason is I want long-term ownership of the outcome — to discover, ship, measure, and iterate on the same product over time, not hand off a strategy and lose the feedback loop. Consulting gave me breadth across many products, but the learning compounds when you live with the telemetry and optimize, and I'd rather own that than advise on it. And I'll own my side of the handoff problem: when a strategy doesn't survive contact with a team, it's usually because the discovery or technical scoping left gaps — which is exactly what in-house ownership closes.)PB0",
    R"PB0(SDTR breadth vs Safed end-to-end ownership (2.5× growth).)PB0",
    R"PB0(Evidence-backed.)PB0",
    R"PB0(Don't blame execution teams ("failed the moment it left my hands") — take responsibility for the scoping gaps; lead with owning the telemetry/iteration loop.)PB0",
  },
  {
    R"PB0(A03)PB0",
    R"PB0(A)PB0",
    R"PB0(Background, Motivation & Fit)PB0",
    R"PB0(3)PB0",
    R"PB0(Why this company and industry)PB0",
    true,
    R"PB0(Fit)PB0",
    R"PB0(I look for companies where a vertical is fused to the brand and there's proprietary data a generic model can't reproduce. Tripla's a good example: the ~1.42% take rate versus the ~25% OTA commission isn't a like-for-like comparison — they're different business models — but it captures the real point, that the hotel keeps the guest relationship, the margin, and the first-party data instead of renting reach. The honest counter is the absolute margin pool is thin and acquisition is expensive, so my job isn't to out-spend OTAs on demand; it's to use AI to lift direct-booking conversion and guest lifetime value so the hotel's own acquisition pays off. What makes it personal is that I already know the Japan inbound market, so I can move on the concrete AI opportunities early.)PB0",
    R"PB0(Tripla take rate 1.42% vs ~25% OTA; re-ground per company.)PB0",
    R"PB0(Evidence-backed for Tripla.)PB0",
    R"PB0(Frame take-rate as a different model, not a smaller commission; have the CAC/unit-economics answer (AI lifts the hotel's own conversion) ready.)PB0",
  },
  {
    R"PB0(A04)PB0",
    R"PB0(A)PB0",
    R"PB0(Background, Motivation & Fit)PB0",
    R"PB0(4)PB0",
    R"PB0(Success / 90-day impact & successful traits)PB0",
    true,
    R"PB0(Execution/Fit)PB0",
    R"PB0(In 90 days, success is one shipped, measurable quick win plus enough understanding of the data and stack to say what's buildable next — and I'd frame day-30 as initial fluency, not mastery. I'd pick the quick win by a simple test: highest-volume, highest-conversion intent where the data already exists and a failure is low-blast-radius, so it moves a real metric without betting the company. If the data turns out to be a fragmented legacy mess — which I'd assume until proven otherwise — I'd say so honestly and protect the timeline by narrowing scope rather than promising a broad "AI concierge" I can't ground. And I'd size the risk that optimizing one intent breaks an upstream integration or a legacy client before I touch it.)PB0",
    R"PB0(Forward-looking plan; the fast-POC capability is real and supports the quick-win claim.)PB0",
    R"PB0(Framework-only — a plan, not a track record.)PB0",
    R"PB0(State dated, proactive milestones; don't assume their team model; handle the legacy-mess and upstream-risk probes head-on.)PB0",
  },
  {
    R"PB0(A05)PB0",
    R"PB0(A)PB0",
    R"PB0(Background, Motivation & Fit)PB0",
    R"PB0(5)PB0",
    R"PB0(Why a Senior PM role / criteria for your next step)PB0",
    false,
    R"PB0(Fit)PB0",
    R"PB0(I've operated as a CPO, COO, and founder, so titles matter less to me than scope — "senior" is the level where I own the product strategy and the technical trade-off decisions with engineering, not just execute a fixed roadmap. My criteria are concrete: a company with proprietary data and a real AI opportunity, a culture that can run controlled experiments and learn from them, and the latitude to own a meaningful product outcome end to end. I'm not looking to own the codebase architecture — that's engineering's — I'm looking to own the what, the why, and the bet. That's where my mix of commercial judgment and technical literacy compounds.)PB0",
    R"PB0(CPO (Pita), COO (Safed), Product Principal (SDTR).)PB0",
    R"PB0(Evidence-backed.)PB0",
    R"PB0(Defuse overqualification: own a product outcome, not the org chart; say explicitly you won't reach into engineering's architecture.)PB0",
  },
  {
    R"PB0(A06)PB0",
    R"PB0(A)PB0",
    R"PB0(Background, Motivation & Fit)PB0",
    R"PB0(6)PB0",
    R"PB0(Why an MBA after a tech/research background)PB0",
    false,
    R"PB0(Background)PB0",
    R"PB0(My engineering and research background gave me rigor and the "how" — but product success depends just as much on deciding what's worth building, the "what" and "why." The MBA gave me the finance and go-to-market tools to govern technical choices by unit economics — off-the-shelf versus fine-tune, build versus buy — instead of technical vanity, and I can put numbers on it: I'd kill a feature when inference cost per use outruns the value it adds. For an AI PM that's much of the job, alongside the user-behavior, risk, and evaluation work — capability only matters tied to revenue, cost, adoption, and feasibility. I value the research foundation because it lets me have those trade-off conversations with engineers credibly.)PB0",
    R"PB0(ESADE MBA + Duke AI PM specialization + research foundation.)PB0",
    R"PB0(Evidence-backed.)PB0",
    R"PB0(Have one concrete unit-economics-killed-a-feature example ready; don't sound dismissive of research; "much of the job," not "the whole job.")PB0",
  },
  {
    R"PB0(A07)PB0",
    R"PB0(A)PB0",
    R"PB0(Background, Motivation & Fit)PB0",
    R"PB0(7)PB0",
    R"PB0(Your biggest career achievement)PB0",
    false,
    R"PB0(Achievement)PB0",
    R"PB0(Building Pita Barcelona — an internationally recognized D2C luxury watch brand — from zero with effectively no paid spend. By co-designing with customers, using pre-sales to validate demand before production, and digital-native manufacturing, we hit profitability in year one and, on the initial investment, roughly 250% ROI in year two, sold out at launch, and earned coverage in the NYT, WSJ, and Bloomberg. What keeps it relevant rather than nostalgic is the method — validate demand before you build, target by behavior, iterate fast — which is how I work on AI products now. I'd keep the AI bridge methodological rather than stack a second metric on it, unless you want the conversational-agent turnaround.)PB0",
    R"PB0(Pita (250% ROI Yr2, NYT/WSJ/Bloomberg). Bridge: Cotomo ~3× retention, methodologically.)PB0",
    R"PB0(Evidence-backed.)PB0",
    R"PB0(Define the ROI denominator inline; foreground the transferable method; don't dwell on press for an AI role.)PB0",
  },
  {
    R"PB0(A08)PB0",
    R"PB0(A)PB0",
    R"PB0(Background, Motivation & Fit)PB0",
    R"PB0(8)PB0",
    R"PB0(Scale of startups you worked at, and where those ventures stand now)PB0",
    false,
    R"PB0(Background)PB0",
    R"PB0(Let me clarify the structure, since it can look tangled: SDTR is my consulting practice, where I've been Product Principal since 2019; Safed K.K. was a full-time interim-COO engagement, 2021 to 2023. On scale I directly managed a cross-functional team of about eight at Safed and coordinated a 30-person remote dev team in Vietnam, and ran a team of around ten at Pita. Pita was acquired and is partner-led now. At Safed I owned the FinTech portfolio end to end as a full-time operating role, not advisory, and drove 2.5× user growth before the engagement closed.)PB0",
    R"PB0(Safed (team of 8 + 30-person dev team; 2.5× growth); Pita (~10, acquired).)PB0",
    R"PB0(Evidence-backed.)PB0",
    R"PB0(Lead with the SDTR-vs-Safed clarification; say Safed was an operating role; have CAC, 6-month stickiness, and the exact Pita transaction structure ready.)PB0",
  },
  {
    R"PB0(A09)PB0",
    R"PB0(A)PB0",
    R"PB0(Background, Motivation & Fit)PB0",
    R"PB0(9)PB0",
    R"PB0(Japanese proficiency / experience with Japanese customers)PB0",
    true,
    R"PB0(Background)PB0",
    R"PB0(I've lived in Tokyo about ten years with a Japanese family, and I work daily in both English and Japanese. Professionally I managed a Japanese headquarters in Japanese for three years and ran stakeholder interviews in Japanese for a Ginza producer, so I've operated in business-Japanese settings, not just survived them — and I'm honest that I keep complex technical documentation in English for precision. I'm comfortable handling stakeholder discussion in Japanese, including with conservative teams, and happy to continue now in Japanese if that's useful.)PB0",
    R"PB0(~10 yrs Tokyo; managed JP HQ in Japanese; Ginza stakeholder interviews in Japanese.)PB0",
    R"PB0(Evidence-backed.)PB0",
    R"PB0(Demonstrate, don't disclaim; have a Japanese answer ready for the inevitable switch — including how you'd persuade conservative stakeholders to move from legacy to AI.)PB0",
  },
  {
    R"PB0(A10)PB0",
    R"PB0(A)PB0",
    R"PB0(Background, Motivation & Fit)PB0",
    R"PB0(10)PB0",
    R"PB0(Do you have an engineering background / justify the business+technical blend)PB0",
    false,
    R"PB0(Background)PB0",
    R"PB0(Yes, a real one — a CS master's, research in multi-agent systems and the semantic web, and I started my career as a software engineer. The semantic-web background is useful here: it's exactly the structured-data and grounding problem that makes RAG and tool-use reliable, so I think in terms of where deterministic structure should bound a stochastic model. I build AI-assisted prototypes myself in Python and Next.js with Claude Code and Cursor, then hand off to engineers to productionize properly — the point isn't to replace them, it's to scope feasibility from experience and ask the right questions about an estimate. The blend is the asset: I can decide what's worth building and discuss the how credibly.)PB0",
    R"PB0(CS master's + AI research + triplaNeo prototype + hackathon.)PB0",
    R"PB0(Evidence-backed.)PB0",
    R"PB0(State exact PhD status; reconcile deterministic semantic-web vs stochastic LLM proactively (you bound the model with structure); drop "vibe-code.")PB0",
  },
  {
    R"PB0(A11)PB0",
    R"PB0(A)PB0",
    R"PB0(Background, Motivation & Fit)PB0",
    R"PB0(11)PB0",
    R"PB0(How you work with and manage distributed, cross-border teams)PB0",
    false,
    R"PB0(Background)PB0",
    R"PB0(My principle is intentionality over proximity — across time zones you engineer the communication instead of relying on hallway osmosis. Concretely: an async pre-read, a tight synchronous decision, and a written decision log, with synchronous time reserved for decisions, not status. At Safed I ran this across eight direct reports on four continents and a 30-person dev team in Vietnam on roughly 13-hour gaps, holding a weekly experiment cadence and a steady release rhythm. Earlier at Transcom I coordinated delivery across 40-plus European centers — a program-coordination role, to be precise, not managing every team inside them — and when the mechanism fails, say an async PRD lacked context and a deploy slipped, I fix the mechanism with a pre-deploy sync on high-risk changes, not just more docs.)PB0",
    R"PB0(Safed (8 reports, 4 continents, 30-person Vietnam team). Alt: Transcom coordination.)PB0",
    R"PB0(Evidence-backed.)PB0",
    R"PB0(Lead with the concrete behavior (pre-read → decision → written log); clarify Transcom was coordination, not direct management of all centers; fix the mechanism, not the symptom.)PB0",
  },
  {
    R"PB0(A12)PB0",
    R"PB0(A)PB0",
    R"PB0(Background, Motivation & Fit)PB0",
    R"PB0(12)PB0",
    R"PB0(Job-hunting status, certainty of moving, availability to join)PB0",
    false,
    R"PB0(Logistics)PB0",
    R"PB0(I'm actively interviewing and in advanced processes elsewhere, nothing finalized, so I'm moving with intent. I can join within four to six weeks for a clean handoff of current engagements; if you need a faster start, I can work to make that happen. I'd rather be straightforward about the timeline so we can plan.)PB0",
    R"PB0(Situational — update to real status.)PB0",
    R"PB0(Evidence-backed (situational).)PB0",
    R"PB0(Keep it clean and confident; don't name competitors or repeat framework language; stay flexible on the start-date probe.)PB0",
  },
  {
    R"PB0(A13)PB0",
    R"PB0(A)PB0",
    R"PB0(Background, Motivation & Fit)PB0",
    R"PB0(13)PB0",
    R"PB0(What you look for from the company / expectations of us)PB0",
    false,
    R"PB0(Fit)PB0",
    R"PB0(Three operational things, framed as what lets me ship for you. Access — to customers, to the data, and real collaboration with engineering, because I can't do discovery or scope feasibility from a distance. Decision authority over a meaningful product outcome, with leadership that backs evidence over the loudest voice. And the ability to run controlled experiments and learn from them — in a vertical where a wrong room rate costs real money, that means safe, staged experimentation with guardrails, not recklessness.)PB0",
    R"PB0(Operational asks (access, authority, safe experimentation).)PB0",
    R"PB0(Evidence-backed.)PB0",
    R"PB0(State operational expectations, not a culture manifesto; caveat "experiment-friendly" hard for a high-stakes vertical so it doesn't sound reckless.)PB0",
  },
  {
    R"PB0(A14)PB0",
    R"PB0(A)PB0",
    R"PB0(Background, Motivation & Fit)PB0",
    R"PB0(14)PB0",
    R"PB0(Would you consider a CEO/COO role in future)PB0",
    false,
    R"PB0(Fit)PB0",
    R"PB0(In the long run, yes — I've held COO and founder roles, so I think with an owner's lens by default. But what that gives you day to day as a PM is prioritization discipline: I weigh technical debt, reliability, and unglamorous work against shiny features because I'm thinking about the whole business, not just what demos well. I'm not treating this as a stepping stone — I'm most useful when I can own a product outcome long enough to see it compound. If my recommendation ever conflicts with the CEO's direction, I bring the evidence, disagree directly, and then commit.)PB0",
    R"PB0(COO (Safed) + founder (Pita/SDTR).)PB0",
    R"PB0(Evidence-backed.)PB0",
    R"PB0(Reassure on flight-risk; reframe owner's mindset as prioritization discipline, not ambition for their seat; drop "survival and growth.")PB0",
  },
  {
    R"PB0(A15)PB0",
    R"PB0(A)PB0",
    R"PB0(Background, Motivation & Fit)PB0",
    R"PB0(15)PB0",
    R"PB0(Where can you help us most (e.g., within the CEO office))PB0",
    false,
    R"PB0(Fit)PB0",
    R"PB0(I'm strongest at turning ambiguous, high-level ideas into validated products — taking something early and unproven and running the discovery, a cheap prototype, and a commercial read so leadership can decide with evidence instead of conviction. Concretely "validated" means signal I can defend: paid pilots, conversion, retention, or a clear demand test, not a polished deck. I'd do this as an internal owner who lives with the result and the codebase over time, not an advisor who hands over a prototype and leaves. My technical literacy lets me run that loop fast and de-risk the expensive bets before they eat the budget.)PB0",
    R"PB0(SDTR principal-product role (ideas → validated POCs).)PB0",
    R"PB0(Evidence-backed.)PB0",
    R"PB0(Never call the CEO's ideas "messy"; define "validated" concretely; frame yourself as an internal owner, not a consultant. ---)PB0",
  },
  {
    R"PB0(B16)PB0",
    R"PB0(B)PB0",
    R"PB0(Product Strategy & Discovery)PB0",
    R"PB0(16)PB0",
    R"PB0(Ideate a new product or feature for our niche)PB0",
    true,
    R"PB0(Strategy/Discovery)PB0",
    R"PB0(Before proposing anything I anchor on a high-frequency, high-severity problem and your behavioral data, not a solution. For a CI/CD tool the sharp pain is red builds that stall a team, so I'd scope an agent that diagnoses the failure and proposes the fix — and I'd size the opportunity transparently (my rough cut was on the order of €80M across ~20,000 mid-market teams, from team count × seats × price, which I'd pressure-test, not assert). The discipline is mapping the pain to one AI capability — here, automated diagnosis — and validating frequency and severity with a cheap off-the-shelf POC before committing ML. And I'd be clear-eyed about why us versus Copilot or the CI vendor owning it — the defensibility has to be real, or it's not worth building.)PB0",
    R"PB0(Genesys Debugger (Autify work-sample ideation); TAM is your estimate. Alt: Cotomo Talk-the-News for a consumer/data niche.)PB0",
    R"PB0(Evidence-backed (real ideation); TAM is an estimate.)PB0",
    R"PB0(Lead with the pain + how you'd validate it; show the TAM as assumptions; have the "why us vs Copilot/CI vendor" answer ready.)PB0",
  },
  {
    R"PB0(B17)PB0",
    R"PB0(B)PB0",
    R"PB0(Product Strategy & Discovery)PB0",
    R"PB0(17)PB0",
    R"PB0(Walk through a past project — problem, contribution, hardest parts, diagnosis)PB0",
    true,
    R"PB0(Strategy/Behavioral)PB0",
    R"PB0(On a Japanese conversational-agent product, the problem was stark: retention stuck in the low single digits — under 3% — on millions of downloads, with no monetization path. My role was product diagnosis and direction, not model engineering; I analyzed the chat and behavioral data, found engagement concentrating on current events and regional topics, and drove a live-retrieval "Talk-the-News" loop targeting exactly those intents. The hardest part was holding voice-grade latency against the compute of live retrieval inside a fixed inference budget on a short runway. We validated it in a matched cohort before scaling, and it roughly tripled retention in that cohort with about 50% longer sessions — directional, since I can't claim a clean randomized control, but consistent enough to act on. I keep the B2B "Digital Senpai" pivot as a separate story so I'm not compressing two projects into one.)PB0",
    R"PB0(Cotomo, ~3× retention (matched cohort) + ~50% longer sessions. Alt: Digital Senpai for a B2B/RAG panel.)PB0",
    R"PB0(Evidence-backed.)PB0",
    R"PB0(Define retention (low-single-digit baseline; matched-cohort lift); own attribution as directional; don't muddle in Digital Senpai.)PB0",
  },
  {
    R"PB0(B18)PB0",
    R"PB0(B)PB0",
    R"PB0(Product Strategy & Discovery)PB0",
    R"PB0(18)PB0",
    R"PB0(Prioritize features for a roadmap or migration)PB0",
    true,
    R"PB0(Strategy)PB0",
    R"PB0(I separate work into innovation, iteration, and operation and allocate top-down, so the roadmap stays a direction tied to the North Star rather than a feature calendar. Within iterations I score with RICE rather than reaching for ICE by reflex — but the senior part is knowing when to override the score: at Safed I pushed a less "efficient"-scoring reliability and onboarding fix ahead of flashier features because the data said churn was concentrated there and the score was undervaluing reach. My fast first filter is whether something solves a high-frequency, high-severity problem or is a nice-to-have. The frameworks order the conversation; the judgment is overriding them when inputs are uncertain — which for a frontier AI feature with no baseline, they always are.)PB0",
    R"PB0(Safed (overrode the score on a reliability/onboarding fix; +55% engagement / −40% TTM).)PB0",
    R"PB0(Evidence-backed.)PB0",
    R"PB0(Lead with one concrete prioritization + when you broke the framework; don't stack RICE/ICE/I-I-O as a textbook recital.)PB0",
  },
  {
    R"PB0(B19)PB0",
    R"PB0(B)PB0",
    R"PB0(Product Strategy & Discovery)PB0",
    R"PB0(19)PB0",
    R"PB0(Zero-to-one discovery, and how you choose a POC / who's involved)PB0",
    false,
    R"PB0(Strategy/Discovery)PB0",
    R"PB0(Let me give a software example rather than the textbook phases. On the conversational agent, zero-to-one meant finding the one engagement loop worth building: I read the behavioral data, saw demand concentrating on current events, and validated a live-content loop in a small matched cohort before scaling — cheap proof before committing. My pattern is: find a high-frequency, high-severity problem with a handful of would-be users, build the thinnest thing that tests the hypothesis — off-the-shelf APIs, a concierge/Wizard-of-Oz, or a fake-door — and only pull in heavy ML once value is proven. For B2B AI, where buyers can't pre-pay for an unbuilt feature, the equivalent of demand validation is letters of intent, paid pilots, design-partner commitments, and fake-door tests on real users. My Pita watch launch is the same pattern in physical form, but I lead with the software version because the feedback loops are closer.)PB0",
    R"PB0(Cotomo Talk-the-News (software-native). Alt: Pita reference-customer pre-sales (physical, secondary).)PB0",
    R"PB0(Evidence-backed.)PB0",
    R"PB0(Lead with a software example; answer the B2B-can't-pre-pay probe directly (LOIs / paid pilots / fake-door); keep Pita secondary.)PB0",
  },
  {
    R"PB0(B20)PB0",
    R"PB0(B)PB0",
    R"PB0(Product Strategy & Discovery)PB0",
    R"PB0(20)PB0",
    R"PB0(Unique value proposition vs competitors / value we bring to customers)PB0",
    false,
    R"PB0(Strategy)PB0",
    R"PB0(The UVP I'd lead with for Tripla is the direct guest relationship: the hotel owns its margin and first-party data instead of renting demand at a ~25% commission. I wouldn't dismiss the OTAs — they do a real job, demand and discovery at scale, and they can add loyalty, fintech, and their own AI — so this isn't "they're obsolete." The durable edge isn't "we have live inventory" — every booking engine has a PMS API — it's the job-to-be-done, turning the hotel's own audience into repeat direct bookings, plus whatever switching cost we build with guest data and workflow integration over time. So I'd compete on owning the guest relationship and the AI that compounds it, not on a feature checklist.)PB0",
    R"PB0(Tripla take rate 1.42% vs ~25% OTA + first-party guest data.)PB0",
    R"PB0(Evidence-backed for Tripla.)PB0",
    R"PB0(Don't claim live inventory is itself the moat (OTAs have PMS APIs too) — the moat is the guest relationship + switching cost; don't over-defend OTAs.)PB0",
  },
  {
    R"PB0(B21)PB0",
    R"PB0(B)PB0",
    R"PB0(Product Strategy & Discovery)PB0",
    R"PB0(21)PB0",
    R"PB0(What should we do about [industry challenge] (e.g., travelers booking via ChatGPT))PB0",
    false,
    R"PB0(Strategy)PB0",
    R"PB0(The instinct is to fear the agent; the better move is to make our data the most machine-readable, transaction-ready source for that hotel's real rates and policies. Agents like ChatGPT collapse trigger and discovery into one step — real — but they don't have live, permissioned inventory and policy data. So the play is structured, canonical data — JSON-LD and schema.org — plus governed APIs and tool endpoints an agent can actually transact against, distributed through the channels and partnerships agents already use. I'd be precise that this isn't "agents auto-discover our MCP server" — MCP is a controlled tool-access protocol, not a public discovery layer; public reach still takes indexing, partnerships, and standards adoption. I prototyped the data-and-grounding side so booking-critical facts come from a source of truth rather than a hallucination.)PB0",
    R"PB0(triplaNeo (a prototype, architecture demo, not production).)PB0",
    R"PB0(Evidence-backed (prototype).)PB0",
    R"PB0(Correct the MCP-discovery misconception yourself; separate "machine-readable data" (real) from "agents auto-discover me" (not how MCP works).)PB0",
  },
  {
    R"PB0(B22)PB0",
    R"PB0(B)PB0",
    R"PB0(Product Strategy & Discovery)PB0",
    R"PB0(22)PB0",
    R"PB0(Why is [product area, e.g. Payments] important, and why interested given your AI background)PB0",
    false,
    R"PB0(Strategy)PB0",
    R"PB0(Payments is the moment of truth — bottom of the funnel where conversion is won or lost, so small gains move revenue directly. There are two distinct AI angles and I'd keep them separate: at checkout, the lever is reducing friction and selectively personalizing an upsell only when it won't add cognitive load and trigger abandonment — a conversion-vs-AOV trade-off I'd A/B test; and in the back office, cash-flow forecasting helps the hotel's operations but is decoupled from the checkout funnel. This sits squarely in my background — I owned wallet and card products at Safed, and as a consultant at Moneytree I proposed AI-driven financial health checks for an app that consolidates financial data. So it's the intersection of my FinTech and AI experience, told precisely.)PB0",
    R"PB0(Safed wallet/card (owned) + Moneytree (consulting) + Pita payments.)PB0",
    R"PB0(Evidence-backed.)PB0",
    R"PB0(Don't muddle back-office forecasting with checkout conversion; state Moneytree was consulting. ---)PB0",
  },
  {
    R"PB0(C23)PB0",
    R"PB0(C)PB0",
    R"PB0(AI/ML Evaluation & QA)PB0",
    R"PB0(23)PB0",
    R"PB0(How do you QA/test a non-deterministic chatbot or AI output)PB0",
    true,
    R"PB0(AI Eval/QA)PB0",
    R"PB0(I treat it as an evaluation system, and the first thing I'd say is that a lot of it is still deterministic — schema validity, citation presence, tool-call correctness, refusal policy, PII redaction, and latency are pass/fail and block a deploy automatically without slowing the team. The probabilistic part I structure as the RAG triad — did we retrieve the right evidence, did the answer stay faithful to it, did it resolve the user's task — scored on a curated, severity-tagged eval set by an LLM-as-judge that I calibrate against human labels and watch for judge drift. The go/no-go is a single severity-weighted decision: a cosmetic miss can ship; a factual, contractual, or PII miss blocks; human review runs sampled and on high-risk cases, not gating every commit. To be precise on maturity, I designed and built this kind of harness for Digital Senpai at pilot scale — task success, retrieval hit-rate, hallucination rate, latency and cost — and engineering owned the production wiring.)PB0",
    R"PB0(Digital Senpai eval harness (designed/built at pilot scale).)PB0",
    R"PB0(Evidence-backed (pilot-scale, not hyperscale MLOps).)PB0",
    R"PB0(Lead with "much of it is deterministic" to defuse the CI-bottleneck probe; calibrate the judge against human labels; state pilot-scale maturity.)PB0",
  },
  {
    R"PB0(C24)PB0",
    R"PB0(C)PB0",
    R"PB0(AI/ML Evaluation & QA)PB0",
    R"PB0(24)PB0",
    R"PB0(Guardrails against hallucinations / PII leaks)PB0",
    true,
    R"PB0(AI Eval/QA)PB0",
    R"PB0(I start from a threat model — I'd name OWASP's LLM Top 10: prompt injection, sensitive-information disclosure, insecure output handling, over-permissioned tools — and defend in layers, with the honest order of importance being access control and grounding first, temperature a minor knob. The real controls: retrieval scoped to only what that user is permitted to see (tenant-aware, least-privilege), instruction/data separation, tool allowlists, output validation, and grounding with citations. On latency I'm careful about the async trap — a guardrail that fires after a streamed response already reached the user doesn't prevent harm — so high-risk intents get a synchronous pre-emission check or aren't streamed, and async is reserved for monitoring and lower-risk flows. I validate with an adversarial red-team suite that reports a leak rate with a real denominator and confidence interval, gate release on it, and give clients a measured commitment and an incident process, never a guarantee.)PB0",
    R"PB0(Deterministic PII redaction (Japanese LM agent / Digital Senpai) — architecture is real; the red-team leak-rate is what you'd gate on.)PB0",
    R"PB0(Evidence-backed on architecture; measured red-team is a framework upgrade.)PB0",
    R"PB0(Name OWASP LLM Top 10; lead with access control + grounding (temperature minor); fix the async hole — high-risk = synchronous pre-emission.)PB0",
  },
  {
    R"PB0(C25a)PB0",
    R"PB0(C)PB0",
    R"PB0(AI/ML Evaluation & QA)PB0",
    R"PB0(25a)PB0",
    R"PB0(Decide an AI feature is ready to release)PB0",
    false,
    R"PB0(AI Eval/QA — release-readiness)PB0",
    R"PB0(Release-readiness is a decision I split into a pre-deployment gate and a phased rollout. The gate is a scorecard, not one number: offline eval pass-rate against a baseline, red-team results, latency and cost-per-interaction, escalation and rollback readiness, monitoring in place — with concrete thresholds, e.g. PII zero-tolerance, factual-error rate under a set bar on the high-severity set, p95 latency within the partner SLA, fallback rate below a ceiling. Then I route a small slice — 1–5% — through shadow or canary; for a chatbot with no ground truth I compare the candidate against the proven baseline and the offline eval set and flag large semantic divergence, and I budget for the extra inference cost shadowing a cohort adds. Release is severity-weighted, never zero-error, with a simpler fallback kept live. I ran the lighter version promoting Digital Senpai PoC→pilot once it cleared measured thresholds.)PB0",
    R"PB0(Digital Senpai PoC→pilot gate (~20% / ~65%). Alt: FinTech severity-weighted rollback.)PB0",
    R"PB0(Evidence-backed on the decision; full shadow/canary mechanics are framework.)PB0",
    R"PB0(Give concrete thresholds; explain how you evaluate shadow without ground truth (vs baseline + eval set); acknowledge shadow's cost.)PB0",
  },
  {
    R"PB0(C25b)PB0",
    R"PB0(C)PB0",
    R"PB0(AI/ML Evaluation & QA)PB0",
    R"PB0(25b)PB0",
    R"PB0(Debug non-deterministic output)PB0",
    false,
    R"PB0(AI Eval/QA — debugging)PB0",
    R"PB0(Because the output is stochastic — and modern hosted models aren't fully deterministic even at temperature 0, thanks to MoE routing and batched execution — I don't treat one bad answer as a fixed bug. I reproduce by logging the full trace — prompt, retrieved context, tool outputs, model and version, parameters — and running N samples to get a failure rate, which also separates a real prompt or retrieval bug from backend jitter. That rate tells me the layer to fix: retrieval (wrong or missing context), generation (unsupported synthesis), or orchestration (a bad tool call). The fix lands there — tighten reranking or metadata filters, force grounding and "I don't know," constrain to structured output, or repair the tool — and the failing case becomes a regression test. And I'm specific about which "drift" I mean — input-distribution, retrieval-corpus, or a silent provider model change — because the response differs.)PB0",
    R"PB0(Digital Senpai eval harness + SLO monitoring.)PB0",
    R"PB0(Evidence-backed on the harness; methodical steps are the articulation.)PB0",
    R"PB0(Don't imply temp-0 is deterministic; lead with measuring a failure rate to separate bug from jitter; define which drift.)PB0",
  },
  {
    R"PB0(C26)PB0",
    R"PB0(C)PB0",
    R"PB0(AI/ML Evaluation & QA)PB0",
    R"PB0(26)PB0",
    R"PB0(AI quality measurement across multiple languages / translated output)PB0",
    false,
    R"PB0(AI Eval/QA — multilingual)PB0",
    R"PB0(I treat translation as a production-quality layer and tier by risk first — a wrong cancellation or payment term is a different class of failure than an awkward adjective, and a wrong Japanese register damages trust disproportionately. Then I split offline from online, because the metrics differ: offline I evaluate on per-language-pair golden sets with reference-based metrics like COMET and chrF plus MQM-style native-speaker review; online, where there's no reference for a live unscripted message, I can't run COMET, so I use reference-free signals — an LLM-as-judge on a per-locale rubric, back-translation as a low-confidence flag, and downstream signals like task completion, CSAT, and support-ticket rate by locale. Low-confidence or high-severity segments route to humans; low-risk static strings use a deterministic localized cache; keigo and brand voice get a rubric plus native review. My hands-on experience here is localization — a multi-language storefront at Pita — so for real-time AI translation QA I'd stand this up as a structured experiment, not claim I've shipped it.)PB0",
    R"PB0(Pita multi-language localization (adjacent, not real-time AI translation QA).)PB0",
    R"PB0(Framework-only for real-time AI translation QA.)PB0",
    R"PB0(COMET needs references — split offline (COMET/golden sets) from online (reference-free: LLM-judge, back-translation, task-success). Don't claim COMET on live dialogue.)PB0",
  },
  {
    R"PB0(C27)PB0",
    R"PB0(C)PB0",
    R"PB0(AI/ML Evaluation & QA)PB0",
    R"PB0(27)PB0",
    R"PB0(Structured data for an AI agent (RAG) — what works / doesn't)PB0",
    false,
    R"PB0(AI Eval/QA — structured data)PB0",
    R"PB0(The core judgment is to separate source-of-truth data from generated text. For live, structured facts — rate, availability, cancellation policy — I don't dump them in a vector store; the agent calls governed tools and APIs that already enforce permissions, the model translates intent into a validated query, and structured results come back deterministically. Retrieval and RAG are for the unstructured content around that, with hybrid search — keyword plus vector plus metadata filters — reranking, freshness checks, and per-user permissioning, and I evaluate retrieval separately from generation, with fine-tuning a last resort. For highly interconnected schemas I'd consider a graph approach (GraphRAG) over flat vector search. What I built was a prototype of the structured source-of-truth layer — JSON-LD over canonical hotel data — not a production integration, and it's grounded in my semantic-web background, the same structured-grounding problem in modern dress.)PB0",
    R"PB0(triplaNeo prototype + semantic-web depth + Digital Senpai RAG.)PB0",
    R"PB0(Evidence-backed (prototype + depth); state prototype maturity.)PB0",
    R"PB0(Lead with tool-calling for live structured data (not vector-dumping); name GraphRAG as an option for relational schemas; say "prototype.")PB0",
  },
  {
    R"PB0(C28)PB0",
    R"PB0(C)PB0",
    R"PB0(AI/ML Evaluation & QA)PB0",
    R"PB0(28)PB0",
    R"PB0(Biggest challenges for this AI product)PB0",
    false,
    R"PB0(AI Eval/QA — product challenges)PB0",
    R"PB0(The biggest challenge is trust, and trust decays through data staleness, so I'd separate the immediate mitigation from the long arc. Right now — without waiting on any platform — you get freshness checks on booking-critical data, retrieval scoped to verified sources, grounding with citations, and a deterministic fallback to "check with the property" for low confidence. The longer arc is consolidating toward a source of truth so improvements compound. If I had to name production risks with a metric each: factual/grounding errors (hallucination rate on the high-severity set), retrieval quality (top-k hit-rate), and cost/latency drift (cost-per-resolved-interaction and p95). And the deepest one is organizational — operating a probabilistic system inside a business process that expects deterministic answers — which is why the eval, monitoring, and fallback discipline matters more than the model choice.)PB0",
    R"PB0(Tripla trust/staleness framing + Digital Senpai data-quality KPIs.)PB0",
    R"PB0(Evidence-backed.)PB0",
    R"PB0(Give the immediate staleness mitigation (don't make it a multi-quarter platform promise); name 2–3 risks with a metric each; drop the reused conversational-agent anecdote here. ---)PB0",
  },
  {
    R"PB0(D29)PB0",
    R"PB0(D)PB0",
    R"PB0(Stakeholder Management & Influence)PB0",
    R"PB0(29)PB0",
    R"PB0(Preferred style of collaborating with engineers)PB0",
    true,
    R"PB0(Stakeholder)PB0",
    R"PB0(My style is mode-dependent. In discovery, engineers are in early to expose constraints, model and tool options, cost, latency, and feasibility — they help decide what we build. In build, I give clear problem framing, success metrics, priority, edge cases, and release criteria, and I don't dictate the how — but I do challenge trade-offs when they affect product risk or business outcome, and I can do that on the merits, like questioning a retrieval or data-model choice that will hurt answer quality. In incidents, I move fast with the smallest empowered group. At Safed, coordinating a 30-person remote dev team, my real value was a concrete intake: every ad-hoc request went through a single backlog with a problem statement and a priority, so nothing bypassed the sprint.)PB0",
    R"PB0(Safed / 30-person Vietnam dev team; single-intake backlog.)PB0",
    R"PB0(Evidence-backed.)PB0",
    R"PB0(Vary the phrasing; show the "challenge trade-offs" line is technical, not positional; give the concrete intake mechanism, not "filtering noise.")PB0",
  },
  {
    R"PB0(D30)PB0",
    R"PB0(D)PB0",
    R"PB0(Stakeholder Management & Influence)PB0",
    R"PB0(30)PB0",
    R"PB0(A time you disagreed with a technical stakeholder — resolution and outcome)PB0",
    true,
    R"PB0(Stakeholder)PB0",
    R"PB0(On the conversational-agent product I disagreed with the CTO's plan to buy celebrity AI voices, because it addressed novelty, not the under-3% retention or the missing monetization — and I'd frame that as a reasonable bet I saw differently, not as them being shallow. I argued for utility grounded in what the behavioral data showed users returned for — the Talk-the-News loop — and brought a thin matched-cohort pilot rather than just an opinion. They initially went with novelty; the pilot I pushed for then showed a clear retention lift in that cohort, which is what actually moved the decision. My real takeaway is about influence, not vindication: bring a cheap experiment early so the data resolves the disagreement instead of seniority.)PB0",
    R"PB0(Cotomo CTO disagreement; cohort-scoped retention lift from Talk-the-News.)PB0",
    R"PB0(Evidence-backed.)PB0",
    R"PB0(Don't make the CTO look shallow or do "I told you so"; the lesson is influence-via-cheap-experiment; keep the lift cohort-scoped.)PB0",
  },
  {
    R"PB0(D31)PB0",
    R"PB0(D)PB0",
    R"PB0(Stakeholder Management & Influence)PB0",
    R"PB0(31)PB0",
    R"PB0(Working with engineers day-to-day when products exist and there's pressure)PB0",
    false,
    R"PB0(Stakeholder — execution under pressure)PB0",
    R"PB0(Under pressure with a live product, my job is to force the trade-offs out loud before work starts — what problem, what metric, what risk, what we're explicitly not doing, and what gate decides whether we continue — so engineering isn't whipsawed by whoever's loudest. I won't commit a hard date until discovery is enough to be confident, and I protect that stance precisely when a big client is pushing, because a date promised on a guess is how teams burn out and ship the wrong thing. For AI work I require a thin-slice plan — smallest shippable experiment, eval criteria, a cost-and-latency budget, a fallback, a decision point — so we don't open-endedly spend the runway. At Safed this ran as a weekly experiment cadence and a release train, and time-to-market dropped about 40% — and to be clear, that came from cutting rework and decision latency, not from cutting QA.)PB0",
    R"PB0(Safed cadence + release train; −40% TTM (rework/decision-latency).)PB0",
    R"PB0(Evidence-backed.)PB0",
    R"PB0(Translate "High-Integrity Commitments" into plain terms; pre-empt the "40% = you cut testing" probe; don't pack five frameworks in.)PB0",
  },
  {
    R"PB0(D32)PB0",
    R"PB0(D)PB0",
    R"PB0(Stakeholder Management & Influence)PB0",
    R"PB0(32)PB0",
    R"PB0(Prove your proposal's value to other stakeholders / PMs)PB0",
    false,
    R"PB0(Stakeholder — influence)PB0",
    R"PB0(I prove value by translating one proposal into each stakeholder's own metric, doing the persuasion one-on-one first, then anchoring everyone to a shared goal and a small measurable pilot instead of a grand vision. On Sparkle, the tokenized-diamond platform, I had to align a manufacturer, an appraisal-and-storage partner, engineering, and VCs, so I framed it in each currency — liquidity for the manufacturer, fractional access for investors, a defensible fee model for finance — and sequenced the most feasible wedge, B2B trading, first. I'd be precise about outcome: it reached working prototype and investor and partner interest, and the hard gate was real — KYC/AML and custody for a regulated asset — which is exactly why B2B-first sequencing was the responsible path, not just the easy one. The transferable point is outcomes over output: I show each party what they can newly do, not a feature list.)PB0",
    R"PB0(Sparkle (led to prototype + interest; pitched VCs/partners).)PB0",
    R"PB0(Evidence-backed.)PB0",
    R"PB0(State Sparkle's real outcome (prototype + interest), not implied revenue; show you handled KYC/AML/custody as the reason for sequencing. ---)PB0",
  },
  {
    R"PB0(E33)PB0",
    R"PB0(E)PB0",
    R"PB0(Metrics & Experimentation)PB0",
    R"PB0(33)PB0",
    R"PB0(Define a North Star metric and sub-metrics (and does it change by stage))PB0",
    true,
    R"PB0(Metrics)PB0",
    R"PB0(A North Star is the single metric that best captures the value the customer actually gets, and under it I name one primary leading indicator, not a dashboard. For a language product like Langets that's Time-to-Proficiency — but since true proficiency is slow and noisy to measure, I'd steer weekly on a validated proxy like completion of calibrated checkpoints, and guard against the vanity trap by only trusting practice-session volume once I've shown it predicts those checkpoints and retention, not just activity. It changes by stage — a validated learning goal in discovery, retention in growth, the business outcome at maturity. AI products do need a few guardrail metrics alongside the North Star — refusal rate, escalation, complaints — so "one leading indicator" means one primary, not blindness to the rest.)PB0",
    R"PB0(Langets (illustrative) + Safed lived North Star (active users).)PB0",
    R"PB0(Evidence-backed (Safed real; Langets illustrative).)PB0",
    R"PB0(Acknowledge guardrail metrics so "one indicator" isn't naive; show how you'd measure proficiency fast (proxy checkpoints) and avoid the vanity trap.)PB0",
  },
  {
    R"PB0(E34)PB0",
    R"PB0(E)PB0",
    R"PB0(Metrics & Experimentation)PB0",
    R"PB0(34)PB0",
    R"PB0(Measure the success of a newly released AI feature)PB0",
    true,
    R"PB0(Metrics (AI))PB0",
    R"PB0(I lead with the metric and the trade-off, because for a matching feature speed is only good if quality holds: success is faster Time-to-First-Match without lowering accepted-match quality or client satisfaction — if acceptance rises but downstream outcomes or repeat usage fall, that's a failure dressed as a win, so I weight match quality and retention above raw speed. I validate against the human-matching baseline with an A/B test sized for enough power to catch quality regressions, or interleaving where a clean cohort split is hard. Because it's AI, system health — latency, cost per resolved match — and the eval gate ride alongside before full traffic, with shadow then canary and a fallback. And I'd be honest the real North Star is a business outcome: winning and keeping paying clients, proxied by those match-quality and satisfaction signals.)PB0",
    R"PB0(VISASQ Time-to-First-Match (role real); shortlist ratio illustrative.)PB0",
    R"PB0(Evidence-backed (role real).)PB0",
    R"PB0(Lead with speed-only-if-quality-holds; don't say "client picks most of it = working"; mention power/interleaving.)PB0",
  },
  {
    R"PB0(E35)PB0",
    R"PB0(E)PB0",
    R"PB0(Metrics & Experimentation)PB0",
    R"PB0(35)PB0",
    R"PB0(Validate the correlation between a sub-metric and the North Star)PB0",
    false,
    R"PB0(Metrics — causal validation)PB0",
    R"PB0(I start by stating the causal hypothesis explicitly, not a gut feeling — "fresher, more relevant content raises session depth, which raises retention" — then test the chain rather than assume correlation is causation. I baseline, define the unit of analysis, and run a matched treatment-vs-control or A/B, explicitly controlling for the obvious confounders — acquisition source, seasonality, content category, novelty, user tenure — because otherwise a lift is just a story. I also stress-test the proxy: long conversation time can signal friction, not engagement, so I cross-validate against the downstream outcome and watch for Goodhart effects. On the conversational agent I did exactly this — hypothesis, matched-cohort pilot, confirmed a retention lift in that cohort, then scaled — and I hold it as directional, not a perfect RCT. Where I can't A/B an outcome, like "did this win a B2B client," I keep the proxy and its causal chain explicit and treat it as a hypothesis under test.)PB0",
    R"PB0(Talk-the-News matched-cohort validation (directional).)PB0",
    R"PB0(Evidence-backed.)PB0",
    R"PB0(Name the specific confounders you controlled for; keep the lift cohort-scoped and call it directional.)PB0",
  },
  {
    R"PB0(E36)PB0",
    R"PB0(E)PB0",
    R"PB0(Metrics & Experimentation)PB0",
    R"PB0(36)PB0",
    R"PB0(A product decision made from data analysis)PB0",
    false,
    R"PB0(Metrics — data-driven decision)PB0",
    R"PB0(At Moneytree — and I'll be precise, this was a consulting engagement on an app that consolidates personal financial data — I ran discovery on the available usage data and found a specific gap: freelancers and SMEs had no forward view of cash flow, which correlates with when they disengage. Rather than add more dashboards, I proposed AI-driven financial health checks and goal-based forecasting in a freemium model. I'd be honest about maturity and causality: this was a validated proposal grounded in the data, and the retention improvement I'd cite is directional for the targeted segment, not a clean isolated experiment I ran to production. The transferable judgment is the one that matters — the data said the missing value wasn't another dashboard, it was a forward-looking view — and I confirm a fix moves a business metric, not just usage.)PB0",
    R"PB0(Moneytree, +40% retention (consulting; directional).)PB0",
    R"PB0(Evidence-backed that it was a real engagement and reported outcome; treat the +40% as directional, not a personally-run RCT.)PB0",
    R"PB0(State Moneytree was consulting; present +40% as directional; don't over-claim shipped ownership. ---)PB0",
  },
  {
    R"PB0(F37)PB0",
    R"PB0(F)PB0",
    R"PB0(Technical Design & Implementation)PB0",
    R"PB0(37)PB0",
    R"PB0(Architecture and tools of your prototype, and how you built it)PB0",
    true,
    R"PB0(Technical)PB0",
    R"PB0(The architectural judgment I'd lead with is the separation: booking-critical facts — rate, availability, cancellation terms — come from deterministic APIs and structured schemas, and generation never invents them; retrieval handles only the unstructured content. I prototyped that as a "truth layer" — JSON-LD/schema.org over canonical hotel data with tool endpoints — to show how an agent could ground on real data instead of hallucinating a rate. I'd be precise that this was a working prototype, not a production integration, and that the hard parts for production are auth, rate-limiting, permissioning, and not exposing live rates in a way competitors can scrape. And I'd correct one common misconception: MCP is a controlled tool-access protocol, not a way ChatGPT "discovers" your endpoint — public reach needs indexing, standards, and partnerships.)PB0",
    R"PB0(triplaNeo (working prototype; Python/Next.js/Vercel). Alt: smartyPants hackathon build.)PB0",
    R"PB0(Evidence-backed (prototype).)PB0",
    R"PB0(Lead with API-vs-generation separation; say "prototype, not production"; correct the MCP-discovery misconception and name the auth/scraping risks.)PB0",
  },
  {
    R"PB0(F38)PB0",
    R"PB0(F)PB0",
    R"PB0(Technical Design & Implementation)PB0",
    R"PB0(38)PB0",
    R"PB0(Build it in terms of resources and time (POC vs production))PB0",
    false,
    R"PB0(Technical — scoping)PB0",
    R"PB0(They're different jobs. A POC proves a hypothesis fast — hours to days on off-the-shelf APIs with a small, high-quality dataset, just to show the value is real. Production is a substantially larger effort — I use a rough 3-to-5× as a planning heuristic, but for strict enterprise SLAs and messy legacy data it can be far more — and the cost is rarely the model; it's the data pipeline, evals, observability, security, and holding latency and cost-per-interaction under real load. I'd budget roughly a 90-day arc — about 30 days on data, 30 on integration, 30 on guardrails, monitoring, and fallback — and state the assumptions: accessible APIs, usable data, engineering bandwidth, scoped use case. And the gate is economic: if production token cost wipes out the unit margin, I change the architecture — cheaper model, caching, routing — or I don't ship.)PB0",
    R"PB0(triplaNeo POC speed; Safed production shipping. Ratio/cadence are heuristics.)PB0",
    R"PB0(Evidence-backed (POC + production shipping).)PB0",
    R"PB0(Call the 3–5× and 30/30/30 heuristics with assumptions; name the economic kill-gate (token cost vs margin).)PB0",
  },
  {
    R"PB0(F39)PB0",
    R"PB0(F)PB0",
    R"PB0(Technical Design & Implementation)PB0",
    R"PB0(39)PB0",
    R"PB0(Resource planning: backend engineers vs ML/data specialists)PB0",
    false,
    R"PB0(Technical — resourcing)PB0",
    R"PB0(I staff by where the uncertainty sits, and I bucket the work as assembly versus R&D before I staff it, because mis-bucketing is how a two-week task becomes six months. Deterministic orchestration around an existing model — auth, lookups, API calls, tool-calling, logging, formatting — is assembly that backend can lead and prototype fast. The R&D bucket — retrieval quality, ranking, translation benchmarks, intent classification, forecasting, fine-tuning, monitoring — needs data and ML specialists in early with explicit iteration budget. The hand-off point is when the work stops being "wire a known capability" and becomes "find out whether the model hits the quality bar" — and when those collide, say retrieval precision is stuck while the plumbing is done, I move budget to the R&D side and make retrieval quality the sprint goal, not more features. On Digital Senpai it ran exactly that way.)PB0",
    R"PB0(Digital Senpai staged plan (backend plumbing vs data-specialist retrieval/eval).)PB0",
    R"PB0(Evidence-backed.)PB0",
    R"PB0(Lead with "staff by uncertainty"; have the retrieval-precision-stuck reallocation example ready.)PB0",
  },
  {
    R"PB0(F40)PB0",
    R"PB0(F)PB0",
    R"PB0(Technical Design & Implementation)PB0",
    R"PB0(40)PB0",
    R"PB0(Do you also code / how much now)PB0",
    false,
    R"PB0(Technical — hands-on)PB0",
    R"PB0(Yes, currently — not fifteen years ago. I build AI-assisted prototypes end to end in Python and Next.js with Claude Code and Cursor; I built the triplaNeo prototype that way and won the AI Voice Agents Hackathon Tokyo 2026 by taking smartyPants from mockup to a working real-time assistant under time pressure. I have a real engineering foundation — software engineer early career, CS master's — but I'm clear about the line: I code to prototype and scope feasibility, not to replace production engineers. The value isn't bravado about estimates — it's that I can read the architecture, spot where it breaks under load, and ask an engineer the right question instead of taking a number on faith.)PB0",
    R"PB0(smartyPants hackathon + triplaNeo prototype.)PB0",
    R"PB0(Evidence-backed.)PB0",
    R"PB0(Drop "vibe-code" and "call BS"; be ready to name a real bug you debugged and the prototype's modules; don't claim production-engineer competence.)PB0",
  },
  {
    R"PB0(F41)PB0",
    R"PB0(F)PB0",
    R"PB0(Technical Design & Implementation)PB0",
    R"PB0(41)PB0",
    R"PB0(RAG / agent-orchestration experience actually delivered)PB0",
    true,
    R"PB0(Technical — delivery proof)PB0",
    R"PB0(Yes — delivered to a working B2B pilot, and I'll define the pilot: a bounded set of corporate users over a fixed period, not full production. On Digital Senpai I built a RAG pipeline over company email and documents — connectors, chunking, embeddings, retrieval — surfaced as onboarding and coaching answers, with PII redaction and an eval harness for retrieval hit-rate, hallucination rate, and latency and cost. It advanced PoC-to-pilot on measured results — roughly 20% fewer onboarding queries to mentors and ~65% faster time-to-answer — and crucially I checked that accuracy held, evaluating retrieval separately from generation so the speed-up wasn't just smoother misinformation. I've also worked the orchestration side — multi-agent automation and tool-use — so I've touched both retrieval and agents, and a deterministic fallback is what made it trustworthy enough to pilot.)PB0",
    R"PB0(Digital Senpai (delivered to pilot; ~20% / ~65%).)PB0",
    R"PB0(Evidence-backed (delivered to pilot).)PB0",
    R"PB0(Define pilot scope (users/period); stress you verified accuracy held, not just speed; keep MCP out unless asked. ---)PB0",
  },
  {
    R"PB0(G42)PB0",
    R"PB0(G)PB0",
    R"PB0(AI Trends & the AI-PM Role)PB0",
    R"PB0(42)PB0",
    R"PB0(Recent AI trends you're experimenting with, and how you keep sharp)PB0",
    false,
    R"PB0(Trends)PB0",
    R"PB0(I track trends by the capability they unlock, not the brand name. The four I actually watch: agentic tool-use (can a system plan, call tools, and recover), inference-time reasoning where you spend more compute for higher quality on hard cases, smaller specialized models with routing for cost, and evaluation-and-observability becoming first-class product infrastructure. The pragmatic version of test-time compute is that you only spend it on the minority of high-stakes intents — which means a cheap, fast classifier deciding stakes up front, a real latency-budget decision. I keep an eye on frontier research but weight building over reading, and I'm wary of forcing the newest protocol into every problem. My most recent hands-on proof is the prototype I built and the hackathon I won, but I'd rather talk about the capability than the trophy.)PB0",
    R"PB0(triplaNeo prototype + hackathon (cited once).)PB0",
    R"PB0(Evidence-backed.)PB0",
    R"PB0(Lead with capabilities, not framework/protocol names; explain test-time compute as a cheap-classifier latency decision; don't re-cite the hackathon as your only proof.)PB0",
  },
  {
    R"PB0(G43)PB0",
    R"PB0(G)PB0",
    R"PB0(AI Trends & the AI-PM Role)PB0",
    R"PB0(43)PB0",
    R"PB0(In the age of AI, how should the PM role change / qualities of a great AI PM)PB0",
    false,
    R"PB0(AI-PM role)PB0",
    R"PB0(The role shifts toward judgment, and the sharpest version of that is being evaluation-first: before engineering writes code, I write the eval rubric, the failure modes, and the acceptance criteria, so a probabilistic system is bounded by a deterministic definition of "good" from day one. That's also how you write a PRD or SLA an exec can sign for an AI feature — not a fixed spec, but acceptable error rates, guardrail metrics, and escalation behavior. The other half is owning ambiguity and release risk: deciding what's worth building, when to use deterministic software instead of a model, and when to override a model's output. The toxic trait is imposition — dictating the how to engineers, or treating model output as ground truth instead of something you evaluate.)PB0",
    R"PB0(Perspective — grounded in your eval-first profile.)PB0",
    R"PB0(Perspective.)PB0",
    R"PB0(Lead with "evaluation-first" as the concrete differentiator; answer the PRD/SLA reality (error rates + guardrails, not fixed specs); keep it concrete, don't lecture.)PB0",
  },
  {
    R"PB0(G44)PB0",
    R"PB0(G)PB0",
    R"PB0(AI Trends & the AI-PM Role)PB0",
    R"PB0(44)PB0",
    R"PB0(How you structure the AI strategy and team)PB0",
    false,
    R"PB0(AI-PM role — org/strategy)PB0",
    R"PB0(I'd structure AI as a thin shared platform plus embedded product squads — and I'd design specifically against the failure mode you're pointing at, the platform becoming a bottleneck. The platform owns shared infrastructure, the eval harness, and governance, and exposes them as self-serve APIs so product squads ship agentic features without queuing for a central team; the squads own the customer outcome. I'd run the work as a portfolio — core use cases that move current KPIs, adjacent ones that open new workflows, and a few experimental bets — with kill criteria. And I'd allocate platform inference cost back to each business unit so the P&L stays honest and nobody treats compute as free. I ran a lighter version at Safed, where a shared data-and-wallet layer fed multiple products rather than each rebuilding it.)PB0",
    R"PB0(Safed shared data/wallet layer; Tripla unified-data framing.)PB0",
    R"PB0(Evidence-backed.)PB0",
    R"PB0(Lead by solving the bottleneck (platform as self-serve API); add portfolio + cost-allocation; don't sound top-down-centralized. ---)PB0",
  },
  {
    R"PB0(H45)PB0",
    R"PB0(H)PB0",
    R"PB0(Misc / Niche)PB0",
    R"PB0(45)PB0",
    R"PB0(How long should an efficient meeting be)PB0",
    false,
    R"PB0(Niche — process)PB0",
    R"PB0(There's no ideal length — the question is whether the meeting has a decision, an alignment, or an unblock to produce. My default is async for status, short meetings for decisions, longer sessions only for genuine discovery or cross-functional trade-offs. When a meeting on an ambiguous AI problem is spinning, I force the outcome by stating the decision we owe, time-boxing the discussion, and if we still can't decide, assigning a directly-responsible owner to make the call with a deadline rather than scheduling another meeting. And I'd adapt to the room — with senior Japanese stakeholders who value a face-to-face briefing, I treat that as relationship and alignment time, not waste, and keep the working cadence async around it.)PB0",
    R"PB0(A stance, plus a concrete force-a-decision mechanism.)PB0",
    R"PB0(Perspective.)PB0",
    R"PB0(Don't sound abrasive about meetings; give the force-a-decision mechanism and the Japanese-exec nuance.)PB0",
  },
  {
    R"PB0(H46)PB0",
    R"PB0(H)PB0",
    R"PB0(Misc / Niche)PB0",
    R"PB0(46)PB0",
    R"PB0(What you learned in the Duke DeFi course / DeFi-Web3 motivation)PB0",
    false,
    R"PB0(Niche — background)PB0",
    R"PB0(What stuck with me was the settlement layer more than the speculation — programmable money and smart contracts as rails that could let agents transact and settle, which is a real question as agentic commerce grows. I'd put it carefully, though: that's a forward-looking thesis, not today's default — most agent transactions will run on cards, bank APIs, and wallets, and anything autonomous in payments runs straight into KYC, AML, and regulatory limits, so a human or hard control stays in the loop. I didn't only study it — I won four awards at ETHGlobal Tokyo 2023, a hackathon, for a tool that automated payouts in fiat and crypto without users needing crypto knowledge. I'd only lead with this if the role is FinTech or Web3; otherwise it's a side interest, not my pitch.)PB0",
    R"PB0(ETHGlobal Tokyo 2023 (hackathon, 4 awards).)PB0",
    R"PB0(Evidence-backed.)PB0",
    R"PB0(Don't overclaim "web3 is how agents will settle"; add the regulated-payments caveat; label it a hackathon; gate relevance to FinTech/Web3 roles. ---)PB0",
  },
  {
    R"PB0(I47)PB0",
    R"PB0(I)PB0",
    R"PB0(Senior AI/ML Design & Strategy (proactive))PB0",
    R"PB0(47)PB0",
    R"PB0(Is AI even the right tool — scoping an AI problem / AI vs a heuristic)PB0",
    false,
    R"PB0(AI design — scoping)PB0",
    R"PB0(My first question is always whether a simpler deterministic solution would do — if a rule, a lookup, or a regex solves it reliably, I don't spend money, latency, and a new failure mode on a model. My suitability check is concrete: is the problem high-value and high-frequency, is there quality data or a grounding source, is the input genuinely unstructured or ambiguous, are errors tolerable and reversible, and can I actually evaluate success. AI earns its place where rules can't keep up and the business can absorb a bounded error rate. Two real calls: I killed a "Talk the Weather" feature because an LLM added almost nothing over a simple forecast widget, and I steered a supply-chain client off premature blockchain toward OCR on export documents first — the unglamorous tool that actually moved throughput.)PB0",
    R"PB0(Cotomo "Talk the Weather" kill; zenPort OCR-over-blockchain.)PB0",
    R"PB0(Evidence-backed (real "don't over-engineer" calls).)PB0",
    R"PB0(Drop the "Desirable/Possible/Probable" alliteration for the plain checklist; lead with the heuristic-first test; have the OCR throughput number ready.)PB0",
  },
  {
    R"PB0(I48)PB0",
    R"PB0(I)PB0",
    R"PB0(Senior AI/ML Design & Strategy (proactive))PB0",
    R"PB0(48)PB0",
    R"PB0(ML/AI system design (recommendation / search-ranking / fraud))PB0",
    false,
    R"PB0(AI design — system architecture)PB0",
    R"PB0(I'd start by naming the decision the system makes — recommend, rank, block, approve, or escalate — because the architecture follows from that, not the reverse. For recommendation or search I'd use a candidate-generation step that narrows the space cheaply, then a heavier ranker on the shortlist, then a re-ranking layer for business rules like diversity and freshness; two-tower retrieval is one common pattern there, not a universal answer, and I'd handle cold-start for new items with content features until behavioral signal accrues. Fraud is a different problem — I wouldn't reach for two-tower; I'd use graph features, anomaly detection, supervised risk scoring, and rules for known attacks, weighting the threshold toward recall and pairing it with a human-review queue, because a missed fraud costs more than a false alarm. The closest I've built is expert-matching at VISASQ — same candidate-then-rank shape, measured on accepted-match quality and Time-to-First-Match — and I'd be clear I haven't built a hyperscale recommender, so I separate the general design from what I've personally shipped.)PB0",
    R"PB0(VISASQ expert-matching (analogous); triplaNeo retrieval+rerank.)PB0",
    R"PB0(Framework-only on a hyperscale recommender; evidence-backed on the analogous matching problem.)PB0",
    R"PB0(Lead with decision-type; say two-tower is "one pattern, not universal"; treat fraud differently (graph/anomaly/rules); name cold-start; separate design knowledge from delivery.)PB0",
  },
  {
    R"PB0(I49)PB0",
    R"PB0(I)PB0",
    R"PB0(Senior AI/ML Design & Strategy (proactive))PB0",
    R"PB0(49)PB0",
    R"PB0(Foundation-model selection / build-vs-buy (cost, latency, quality))PB0",
    false,
    R"PB0(AI design — model selection)PB0",
    R"PB0(I decide with a build-versus-buy lens governed by economics, but I'd correct the binary: "build" almost never means training a frontier model from scratch — it means fine-tuning, distilling, adapters, or self-hosting — and I'd fine-tune for several reasons beyond a moat: privacy, latency, cost at volume, controllability, or a domain format the base model handles poorly. Default is buy via API to get to market, then move work to smaller or self-hosted models where privacy or unit cost demands it — a model portfolio with routing, not one model. The cautionary case I worked: a product had built its own model and the spend wasn't tied to a defensible revenue path — and to be precise, I was advising on product strategy there, I didn't run that training. My reference point is that you win on the layer above the model — retrieval, workflow, distribution — unless you genuinely are the model company.)PB0",
    R"PB0(Cotomo build-your-own analysis (your strategic take; advisory role) vs Perplexity buy-and-win-on-RAG.)PB0",
    R"PB0(Evidence-backed (the analysis is yours; the build wasn't).)PB0",
    R"PB0(Say "build" usually = fine-tune/distill/self-host; give reasons beyond moat (privacy/latency/cost/control); state your advisory role on the from-scratch build; mention model portfolio/routing.)PB0",
  },
  {
    R"PB0(I50)PB0",
    R"PB0(I)PB0",
    R"PB0(Senior AI/ML Design & Strategy (proactive))PB0",
    R"PB0(50)PB0",
    R"PB0(Prompt engineering vs RAG vs fine-tuning)PB0",
    false,
    R"PB0(AI design — technique trade-off)PB0",
    R"PB0(I diagnose by what's actually missing, not by preference. If the model lacks current or private knowledge, the answer is RAG or tools, not fine-tuning — and a real modern option is loading many high-quality examples into a long context window (many-shot in-context learning), which often matches fine-tuning for domain adaptation without the rigidity. If the model knows the facts but behaves inconsistently — wrong format, wrong style, weak on a narrow classification — that's where fine-tuning earns its place, and with LoRA or adapters it's cheaper than people assume. If the task needs actions, that's tool-calling or agents. I made this call on the conversational agent: a RAG news loop over fine-tuning, because the content changes daily and a cheaply-updated index beats retraining on stale data — and I'd pair it with freshness metadata and a re-index cadence so a breaking story is retrievable fast.)PB0",
    R"PB0(Talk-the-News RAG-over-fine-tune (real); Digital Senpai RAG.)PB0",
    R"PB0(Evidence-backed.)PB0",
    R"PB0(Reframe as knowledge (RAG/tools) vs behavior/format (fine-tune) vs workflow (agents); note LoRA/many-shot; add freshness handling.)PB0",
  },
  {
    R"PB0(I51)PB0",
    R"PB0(I)PB0",
    R"PB0(Senior AI/ML Design & Strategy (proactive))PB0",
    R"PB0(51)PB0",
    R"PB0(Responsible AI, privacy, and compliance for B2B in Japan)PB0",
    false,
    R"PB0(AI design — responsible AI)PB0",
    R"PB0(For B2B in Japan I'd lead with the insight that compliance and data residency are a sales gate for conservative enterprise buyers, not just legal hygiene — clear that first. As an operating model I'd name NIST's AI Risk Management Framework — govern, map, measure, manage — and OWASP's LLM Top 10 for the security side, rather than abstract ethics principles. Concretely: data minimization and consent, tenant isolation, PII handling that's layered (NER plus a policy engine, not just regex), audit logs, and access control; and I'd flag the genuinely hard one under APPI — honoring deletion when personal data has already entered a vector store or a fine-tune, which needs design up front. On explainability I'm precise: SHAP-style methods don't explain an LLM well, so "explainable" here means source attribution, decision logs, and traceability. I've operated under regulatory constraint in FinTech — KYC/AML and risk sign-offs at Safed — and I'm careful not to conflate that with AI governance; they're different disciplines, but both taught me to design controls in from the start.)PB0",
    R"PB0(Safed KYC/AML + risk sign-offs (regulatory constraint); PII redaction (Japanese LM agent / Digital Senpai); Japan sales-gate insight.)PB0",
    R"PB0(Evidence-backed on operating-under-constraint + privacy-by-design; APPI/governance specifics are framework-led.)PB0",
    R"PB0(Name NIST AI RMF + OWASP LLM Top 10; don't conflate KYC/AML with AI governance; give PII specifics (NER+policy) and the vector-store deletion problem; explainability = attribution/logs, not SHAP.)PB0",
  },
  {
    R"PB0(I52)PB0",
    R"PB0(I)PB0",
    R"PB0(Senior AI/ML Design & Strategy (proactive))PB0",
    R"PB0(52)PB0",
    R"PB0(Human-in-the-loop / graduated autonomy)PB0",
    false,
    R"PB0(AI design — autonomy)PB0",
    R"PB0(I graduate autonomy by the risk of the action, not a calendar or a single accuracy number — the ladder is suggest-only, human-approval, review-by-exception, auto-execute-with-rollback, and only then full autonomy, and the gate depends on reversibility, blast radius, financial and legal exposure, and auditability. A 95% acceptance rate doesn't earn autonomy if the other 5% can do irreversible harm. On Digital Senpai the system suggested and humans approved while I measured acceptance, and human corrections fed an improvement and eval-data queue — I'd call it that, not a "retraining queue," since for a RAG system the fixes were usually retrieval, prompts, and eval cases, not model retraining. And I'd actively guard the feedback loop against reviewer bias and label noise, because overloaded humans accept wrong answers and you can poison your own data.)PB0",
    R"PB0(Digital Senpai graduated rollout + improvement/eval-data queue.)PB0",
    R"PB0(Evidence-backed.)PB0",
    R"PB0(Gate autonomy by reversibility/blast-radius, not just acceptance %; call it an improvement/eval-data queue (not "retraining" for RAG); name reviewer-bias/label-noise risk.)PB0",
  },
  {
    R"PB0(I53)PB0",
    R"PB0(I)PB0",
    R"PB0(Senior AI/ML Design & Strategy (proactive))PB0",
    R"PB0(53)PB0",
    R"PB0(Latency & cost optimization)PB0",
    false,
    R"PB0(AI design — efficiency)PB0",
    R"PB0(I treat a cost-per-interaction and a latency budget as release-gate criteria, then design the inference path to fit. The main levers I'd reach for: a router that sends easy queries to a small cheap model and hard ones to the expensive model, semantic caching for repeated inputs with careful invalidation so users never get a stale or out-of-context cached answer, right-sized models, streaming, and async for anything non-real-time. The router itself is usually a lightweight classifier, so it adds a little latency I'd budget for, and I'd guard against the cheap model silently degrading quality with the same evals. I'm honest about depth: techniques like distillation, speculative decoding, and prefill/KV-cache optimization are real production levers I understand and would lean on engineering for, not things I've hand-tuned myself. The concrete experience I do have is the real-time voice agent, where holding latency against retrieval compute drove the architecture.)PB0",
    R"PB0(Cotomo voice latency vs retrieval-compute trade-off (real).)PB0",
    R"PB0(Evidence-backed on the trade-off; router/caching/infra are framework you'd apply, with deep infra owned by engineering.)PB0",
    R"PB0(Lead with budget-as-gate; caveat cache invalidation and router latency; don't claim to hand-tune compression/speculative decoding — frame as awareness + lean on engineering.)PB0",
  },
  {
    R"PB0(I54)PB0",
    R"PB0(I)PB0",
    R"PB0(Senior AI/ML Design & Strategy (proactive))PB0",
    R"PB0(54)PB0",
    R"PB0(Agentic system design)PB0",
    false,
    R"PB0(AI design — agents)PB0",
    R"PB0(I design agents as a bounded control loop — plan, call an allowlisted tool, observe, verify the result against explicit success criteria, then escalate or stop — and I'm wary of leaning on "the agent reflects on itself" as if introspection guarantees correctness; the real safety comes from verification, constraints, and a state machine that hard-codes which actions are legal in which state. Often a single orchestrator with well-scoped tools is safer and more debuggable than a swarm of agents, so I don't reach for multi-agent unless the task needs it. I'd prevent loops and unsafe actions with step budgets, idempotency, per-tool permissions, full tracing, and rollback. This connects directly to my PhD work — classic multi-agent coordination like contract-net and blackboard architectures is the same problem of decomposition, bidding, and shared state that LLM orchestration is rediscovering — so I bring the coordination theory, applied with modern guardrails and evals.)PB0",
    R"PB0(Multi-agent B2B automation + PhD in multi-agent systems + MCP orchestration.)PB0",
    R"PB0(Evidence-backed.)PB0",
    R"PB0(Replace "reflection" with verification + state machine + allowlists + rollback; note single-orchestrator is often safer; connect contract-net/blackboard to modern orchestration to turn the "dated PhD" probe into a strength.)PB0",
  },
  {
    R"PB0(I55)PB0",
    R"PB0(I)PB0",
    R"PB0(Senior AI/ML Design & Strategy (proactive))PB0",
    R"PB0(55)PB0",
    R"PB0(AI business case / ROI)PB0",
    false,
    R"PB0(AI design — commercial)PB0",
    R"PB0(I model AI ROI at the unit level, not as a vague payback: value per successful task — revenue lifted or labor saved — minus the full cost to serve, which has to include inference, human review, eval, maintenance, and incident handling, plus the ongoing data toil of cleaning and re-indexing. For a support agent, for instance, I'd track cost per resolved ticket, containment rate, escalation quality, and the churn or refund impact, and I won't ship if the margin disappears once review and infra costs are in. I anchor this in real operating experience — I co-owned the P&L at Safed, modeling development and infrastructure spend against revenue and ROI scenarios, which is software unit economics, not a one-off project return. And cost-per-interaction often is the deciding variable: at scale it's what determines whether the ROI is actually real.)PB0",
    R"PB0(Safed P&L co-ownership + infra-cost modeling.)PB0",
    R"PB0(Evidence-backed.)PB0",
    R"PB0(Lead with unit economics (value-per-task − full cost-to-serve incl. data toil); anchor on Safed P&L, not the Pita watch ROI; name a concrete equation.)PB0",
  },
  {
    R"PB0(I56)PB0",
    R"PB0(I)PB0",
    R"PB0(Senior AI/ML Design & Strategy (proactive))PB0",
    R"PB0(56)PB0",
    R"PB0(Data strategy for an AI product)PB0",
    false,
    R"PB0(AI design — data)PB0",
    R"PB0(My strategy is data-centric — for most products, quality data beats a bigger model — but I'd immediately qualify the flywheel, because it's the part people get wrong. Implicit signals like clicks and acceptances are biased observational data — selection, position, and survivorship bias, and the model shaping its own future inputs — so I treat them as signals to validate and debias, not clean labels that auto-retrain anything. I'd put data contracts in place with engineering — schema, freshness, ownership, lineage, PII rules, acceptable missingness — and treat coverage and freshness as first-class KPIs, not backend details. On Digital Senpai I ran data-quality KPIs for coverage, freshness, and retrieval hit-rate, with a human flag-and-annotate loop feeding an improvement and eval-data queue. The discipline is that the data pipeline and its feedback loops are as much the product as the model — but only if the loop is clean.)PB0",
    R"PB0(Digital Senpai data-quality KPIs + flag/annotate loop.)PB0",
    R"PB0(Evidence-backed.)PB0",
    R"PB0(Qualify the flywheel with feedback bias (selection/position/survivorship/model-induced); add data contracts; call it an improvement/eval-data queue. ---)PB0",
  },
  {
    R"PB0(J57)PB0",
    R"PB0(J)PB0",
    R"PB0(AI Operations, Trust & Commercialization (new — proactive, surfaced by critique))PB0",
    R"PB0(57)PB0",
    R"PB0(Incident response for an AI failure)PB0",
    false,
    R"PB0(AI ops — incident handling)PB0",
    R"PB0(I'd run it like any severity incident, with the AI specifics layered in. First minutes: classify severity by real-world harm — a wrong cancellation policy that cost a guest money is high — then contain by disabling or rolling back the offending path to the deterministic fallback so it can't recur while we investigate. In the first 24 hours: remediate the affected customer directly, do root-cause from the logged trace (prompt, retrieval, model version, tool calls) to find whether it was retrieval, generation, or data staleness, and keep the partner informed with a measured account, looping in legal and compliance if there's liability. Then the durable step: add the failing case to the eval set as a regression test and ship the prevention, so the same class of error is gated next time. I've made the severity-weighted version of this call in FinTech — rolling back a high-impact path while patching low-impact issues on the next release — which is exactly this muscle.)PB0",
    R"PB0(Safed/Shinobi severity-weighted rollback; Digital Senpai trace/eval discipline.)PB0",
    R"PB0(Framework-led with a real severity-weighted-rollback anchor (no public AI incident run).)PB0",
    R"PB0(Lead with classify→contain→remediate→RCA→prevent; name the fallback and the eval-set update; don't promise zero incidents.)PB0",
  },
  {
    R"PB0(J58)PB0",
    R"PB0(J)PB0",
    R"PB0(AI Operations, Trust & Commercialization (new — proactive, surfaced by critique))PB0",
    R"PB0(58)PB0",
    R"PB0(Enterprise AI procurement / buyer trust)PB0",
    false,
    R"PB0(AI ops — procurement & trust)PB0",
    R"PB0(For conservative enterprise buyers, especially in Japan, the AI doesn't get approved on a demo — it gets approved on trust, so I'd prepare the procurement package as a product deliverable. That's the security-questionnaire answers, data handling and retention terms, whether and how data is used for training, model-provider terms, audit logs, admin controls and human override, SLAs, and the relevant posture — SOC 2 or ISO if we have it, plus APPI and cross-border-transfer handling for Japan. Buyers increasingly want written limits on liability for hallucinated output or IP infringement, so I'd work with legal to offer a bounded, measured commitment — an incident process and a defined error budget — rather than an uncapped guarantee. And I'd give them an evaluation report on how we measure quality and safety, so they trust the system, not just the pitch. This is the sales-gate reality I learned shipping regulated FinTech products.)PB0",
    R"PB0(Regulated-FinTech + Japan sales-gate experience; compliance background.)PB0",
    R"PB0(Framework-led, grounded in real regulated-B2B / Japan experience (no formal SOC2/ISO program run).)PB0",
    R"PB0(Frame trust as a product deliverable (questionnaire, retention, audit logs, override, eval report); offer bounded liability + incident process, not an uncapped guarantee.)PB0",
  },
  {
    R"PB0(J59)PB0",
    R"PB0(J)PB0",
    R"PB0(AI Operations, Trust & Commercialization (new — proactive, surfaced by critique))PB0",
    R"PB0(59)PB0",
    R"PB0(Model/provider operational risk, portability & Day-2 drift)PB0",
    false,
    R"PB0(AI ops — vendor & operations)PB0",
    R"PB0(I assume the provider will change pricing, degrade, change terms, or have an outage, and design so none of those is an emergency. The core is an abstraction layer over the model so we can swap providers, a cross-provider eval benchmark so "is the new one good enough" is a measured question not a guess, cost and latency monitoring with alerts, caching where safe, and a fallback model for outages. A specific Day-2 trap I watch for: a provider silently updating model weights can quietly skew a calibrated LLM-as-judge baseline, so I re-run the eval calibration when the upstream model changes rather than trusting yesterday's numbers. On terms, I'd review data-retention and training-use clauses before committing sensitive data. The point is no-regret portability — we're never one vendor decision away from a broken product.)PB0",
    R"PB0(Build-vs-buy/portfolio reasoning (card 49) + eval discipline.)PB0",
    R"PB0(Framework-led (sound design; not operated at scale).)PB0",
    R"PB0(Lead with abstraction + cross-provider eval + fallback; name the silent-weight-change → re-calibrate-evals trap; review retention/training-use terms.)PB0",
  },
  {
    R"PB0(J60)PB0",
    R"PB0(J)PB0",
    R"PB0(AI Operations, Trust & Commercialization (new — proactive, surfaced by critique))PB0",
    R"PB0(60)PB0",
    R"PB0(Data rights & consent — using customer data for training and evaluation)PB0",
    false,
    R"PB0(AI ops — data governance)PB0",
    R"PB0(The default answer is: not without a clear basis, and I'd separate the cases. Production logs are not automatically training or eval data — using customer conversations to improve the system needs consent or a contractual basis, and for B2B in Japan that's usually a per-tenant question, not a blanket right. So I'd design for it: tenant isolation, a documented consent and opt-out path, anonymization and PII stripping before anything enters an eval or training set, a retention policy, and a hard separation between production logs and improvement data with access controls. Where we don't have rights to real data, I'd lean on synthetic or held-out curated sets for evaluation. This matters commercially as much as legally — getting it wrong is exactly what fails a Japanese enterprise security review.)PB0",
    R"PB0(PII redaction work + Japan B2B sales-gate insight.)PB0",
    R"PB0(Framework-led, grounded in real PII / Japan-B2B experience.)PB0",
    R"PB0(Lead with "logs ≠ training/eval data; needs consent/contract, per-tenant in Japan"; name anonymization, opt-out, retention, prod/eval separation, synthetic fallback.)PB0",
  },
  {
    R"PB0(J61)PB0",
    R"PB0(J)PB0",
    R"PB0(AI Operations, Trust & Commercialization (new — proactive, surfaced by critique))PB0",
    R"PB0(61)PB0",
    R"PB0(Pricing & packaging for an AI feature)PB0",
    false,
    R"PB0(AI commercialization — pricing)PB0",
    R"PB0(I'd start from the value metric — what the customer is actually buying — and the cost-to-serve, because AI features have real marginal cost, so margin discipline matters more than in classic SaaS. For an assistive feature where value scales with use, usage- or outcome-based pricing per resolved task aligns price with value, but it hurts buyer predictability and exposes you if costs spike, so in practice I often prefer a tiered or seat-plus-usage model with usage caps that protects margin while staying legible to procurement. Outcome-based pricing is attractive but risky when attribution is fuzzy, so I'd only use it where the outcome is cleanly measurable. I'd protect the downside with usage caps and cost-per-interaction monitoring, and use a paid pilot rather than a free beta so we learn willingness-to-pay early. The anchor is that price has to clear cost-to-serve with margin, or the feature is a loss leader by accident.)PB0",
    R"PB0(MBA/commercial judgment + Safed P&L; Moneytree freemium design.)PB0",
    R"PB0(Framework-led, grounded in real commercial/P&L experience (no specific AI-pricing case claimed).)PB0",
    R"PB0(Start from value metric + cost-to-serve; weigh usage vs seat vs outcome with the predictability/margin trade-offs; protect margin with caps; paid pilot over free beta.)PB0",
  },
  {
    R"PB0(J62)PB0",
    R"PB0(J)PB0",
    R"PB0(AI Operations, Trust & Commercialization (new — proactive, surfaced by critique))PB0",
    R"PB0(62)PB0",
    R"PB0(Legacy-infrastructure migration & corporate gridlock (Japan enterprise))PB0",
    false,
    R"PB0(AI delivery — legacy & org)PB0",
    R"PB0(In Japanese enterprise and hospitality, the data you need is often trapped in siloed, poorly-documented legacy systems with strict access rules, so I treat this as an organizational problem as much as a technical one. Technically I wouldn't propose a big-bang re-platform; I'd use a strangler pattern — stand up a thin, well-governed access layer or API over the legacy system, build the AI feature against that, and migrate incrementally so we ship value without a multi-year prerequisite. The first 30 days are mostly access and trust: mapping data owners, permissions, and quality, and earning the cooperation of the team that guards the legacy system, because in Japan that relationship is the actual gate. I'd sequence the highest-value, lowest-access-friction use case first to create a proof point, then use it to unlock the harder integrations. I've done the adjacent version steering a supply-chain client to start with OCR over existing export documents rather than waiting on a full systems overhaul.)PB0",
    R"PB0(zenPort OCR-first incremental delivery + 10y Japan enterprise context.)PB0",
    R"PB0(Framework-led, grounded in real Japan-enterprise / incremental-delivery experience.)PB0",
    R"PB0(Lead with strangler-pattern + access-layer (not big-bang); name the org gate (data owners / legacy-team relationship) as the real first-30-days work; sequence by value × access-friction. --- # PART 2 — The questions you ask them Pick 3–5 and lead with #1. Phrased so you can say them close to verbatim. 1. "Six months in, what would make you say this hire was clearly the right one?" — Lead with this. Surfaces the real success criteria and reads senior. 2. "What's the biggest struggle right now — generating ideas, building POCs, or shipping to production?" — Pinpoints where you add value. Best with CEO/founder or hiring manager. 3. "Where does AI sit in the strategy — a core platform or a feature — and which in-house AI bets are you most excited about?" — Tells you whether AI here is real or decorative. Best with leadership. 4. "How do customer or operator insights reach the roadmap today — what's the path from a real pain to a prioritized build?" — Probes discovery rigor without lecturing. Best with a product leader. 5. "What's your vision for the next one, three, and five years, and where's the market expansion?" — Long-horizon interest. Good with founders. 6. "In the first quarter, what would I own outright versus influence?" — Clarifies real scope and autonomy. 7. "How are decisions made when AI work is uncertain — who calls it, and how do you avoid over-investing in unproven bets?" — Shows you've shipped AI. Best with a CTO/technical panel. 8. (Late / logistics) "Does the company support AI tooling, learning, and conference attendance?" — Fine as a closing question. Ask carefully or skip. "Why did [predecessor] leave?" / "Why have so many people left recently?" read as red-flag-hunting. Soften to "How has the team evolved over the last year?" Selection by room. CEO/founder → #1 + #2 + #5. Product leader → #1 + #4 + #6. CTO/technical panel → #1 + #3 + #7. --- End of prep sheet (v2, hardened). Coverage: Part 1 categories A–J (63 cards: A–H = 46 with item 25 split = 47; Section I = 47–56; Section J = 57–62) + Part 2 (8 questions to ask). All metrics locked and defensibly framed; maturity stated in each answer; standards named only where defensible.)PB0",
  },
  {
    R"PB0(Q01)PB0",
    R"PB0(Q)PB0",
    R"PB0(Questions to ask interviewer)PB0",
    R"PB0(1)PB0",
    R"PB0(Six months in, what would make you say this hire was clearly the right one?)PB0",
    true,
    R"PB0(Questions to ask interviewer)PB0",
    R"PB0(Lead with this. Surfaces the real success criteria and reads senior.)PB0",
    R"PB0()PB0",
    R"PB0(Perspective)PB0",
    R"PB0(Use selectively by interviewer type.)PB0",
  },
  {
    R"PB0(Q02)PB0",
    R"PB0(Q)PB0",
    R"PB0(Questions to ask interviewer)PB0",
    R"PB0(2)PB0",
    R"PB0(What's the biggest struggle right now — generating ideas, building POCs, or shipping to production?)PB0",
    false,
    R"PB0(Questions to ask interviewer)PB0",
    R"PB0(Pinpoints where you add value. Best with CEO/founder or hiring manager.)PB0",
    R"PB0()PB0",
    R"PB0(Perspective)PB0",
    R"PB0(Use selectively by interviewer type.)PB0",
  },
  {
    R"PB0(Q03)PB0",
    R"PB0(Q)PB0",
    R"PB0(Questions to ask interviewer)PB0",
    R"PB0(3)PB0",
    R"PB0(Where does AI sit in the strategy — a core platform or a feature — and which in-house AI bets are you most excited about?)PB0",
    false,
    R"PB0(Questions to ask interviewer)PB0",
    R"PB0(Tells you whether AI here is real or decorative. Best with leadership.)PB0",
    R"PB0()PB0",
    R"PB0(Perspective)PB0",
    R"PB0(Use selectively by interviewer type.)PB0",
  },
  {
    R"PB0(Q04)PB0",
    R"PB0(Q)PB0",
    R"PB0(Questions to ask interviewer)PB0",
    R"PB0(4)PB0",
    R"PB0(How do customer or operator insights reach the roadmap today — what's the path from a real pain to a prioritized build?)PB0",
    false,
    R"PB0(Questions to ask interviewer)PB0",
    R"PB0(Probes discovery rigor without lecturing. Best with a product leader.)PB0",
    R"PB0()PB0",
    R"PB0(Perspective)PB0",
    R"PB0(Use selectively by interviewer type.)PB0",
  },
  {
    R"PB0(Q05)PB0",
    R"PB0(Q)PB0",
    R"PB0(Questions to ask interviewer)PB0",
    R"PB0(5)PB0",
    R"PB0(What's your vision for the next one, three, and five years, and where's the market expansion?)PB0",
    false,
    R"PB0(Questions to ask interviewer)PB0",
    R"PB0(Long-horizon interest. Good with founders.)PB0",
    R"PB0()PB0",
    R"PB0(Perspective)PB0",
    R"PB0(Use selectively by interviewer type.)PB0",
  },
  {
    R"PB0(Q06)PB0",
    R"PB0(Q)PB0",
    R"PB0(Questions to ask interviewer)PB0",
    R"PB0(6)PB0",
    R"PB0(In the first quarter, what would I own outright versus influence?)PB0",
    false,
    R"PB0(Questions to ask interviewer)PB0",
    R"PB0(Clarifies real scope and autonomy.)PB0",
    R"PB0()PB0",
    R"PB0(Perspective)PB0",
    R"PB0(Use selectively by interviewer type.)PB0",
  },
  {
    R"PB0(Q07)PB0",
    R"PB0(Q)PB0",
    R"PB0(Questions to ask interviewer)PB0",
    R"PB0(7)PB0",
    R"PB0(How are decisions made when AI work is uncertain — who calls it, and how do you avoid over-investing in unproven bets?)PB0",
    false,
    R"PB0(Questions to ask interviewer)PB0",
    R"PB0(Shows you've shipped AI. Best with a CTO/technical panel.)PB0",
    R"PB0()PB0",
    R"PB0(Perspective)PB0",
    R"PB0(Use selectively by interviewer type.)PB0",
  },
  {
    R"PB0(Q08)PB0",
    R"PB0(Q)PB0",
    R"PB0(Questions to ask interviewer)PB0",
    R"PB0(8)PB0",
    R"PB0(Late / logistics: Does the company support AI tooling, learning, and conference attendance?)PB0",
    false,
    R"PB0(Questions to ask interviewer)PB0",
    R"PB0(Fine as a closing question.)PB0",
    R"PB0()PB0",
    R"PB0(Perspective)PB0",
    R"PB0(Use selectively by interviewer type.)PB0",
  },
};

const Drill kDrills[] PROGMEM = {
  {
    R"PB0(A01-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(A01)PB0",
    R"PB0(What confidence framing belongs with: Self-introduction / career & recent work?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card A01 is tagged: Evidence-backed.)PB0",
  },
  {
    R"PB0(A01-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(A01)PB0",
    R"PB0(What is the main risk in a weak answer to: Self-introduction / career & recent work?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    0,
    R"PB0(Over-talk risk — three strengths, one recent signal, stop. Have your exact PhD status ready so "research background" can't read as evasive.)PB0",
  },
  {
    R"PB0(A01-hostile)PB0",
    R"PB0(hostile_followup)PB0",
    R"PB0(A01)PB0",
    R"PB0(Interviewer pushes on 'Self-introduction / career & recent work': Over-talk risk — three strengths, one recent signal, stop. Have your exact PhD status ready so "research background" can't read as evasive.)PB0",
    {
      R"PB0()PB0",
      R"PB0()PB0",
      R"PB0()PB0",
      R"PB0()PB0",
    },
    0,
    0,
    R"PB0(Best response principle: RAG pilots (Digital Senpai) as the production-grade signal; hackathon as recency; Pita (250% ROI Yr2) and Cotomo (~3× retention) as range. Alt: lead with Safed COO for operator-heavy roles.)PB0",
  },
  {
    R"PB0(A02-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(A02)PB0",
    R"PB0(What confidence framing belongs with: Why leave consulting for an in-house role?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card A02 is tagged: Evidence-backed.)PB0",
  },
  {
    R"PB0(A02-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(A02)PB0",
    R"PB0(What is the main risk in a weak answer to: Why leave consulting for an in-house role?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    3,
    R"PB0(Don't blame execution teams ("failed the moment it left my hands") — take responsibility for the scoping gaps; lead with owning the telemetry/iteration loop.)PB0",
  },
  {
    R"PB0(A02-hostile)PB0",
    R"PB0(hostile_followup)PB0",
    R"PB0(A02)PB0",
    R"PB0(Interviewer pushes on 'Why leave consulting for an in-house role': Don't blame execution teams ("failed the moment it left my hands") — take responsibility for the scoping gaps; lead with owning the telemetry/iteration loop.)PB0",
    {
      R"PB0()PB0",
      R"PB0()PB0",
      R"PB0()PB0",
      R"PB0()PB0",
    },
    0,
    0,
    R"PB0(Best response principle: SDTR breadth vs Safed end-to-end ownership (2.5× growth).)PB0",
  },
  {
    R"PB0(A03-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(A03)PB0",
    R"PB0(What confidence framing belongs with: Why this company and industry?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card A03 is tagged: Evidence-backed for Tripla.)PB0",
  },
  {
    R"PB0(A03-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(A03)PB0",
    R"PB0(What is the main risk in a weak answer to: Why this company and industry?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    4,
    R"PB0(Frame take-rate as a different model, not a smaller commission; have the CAC/unit-economics answer (AI lifts the hotel's own conversion) ready.)PB0",
  },
  {
    R"PB0(A03-hostile)PB0",
    R"PB0(hostile_followup)PB0",
    R"PB0(A03)PB0",
    R"PB0(Interviewer pushes on 'Why this company and industry': Frame take-rate as a different model, not a smaller commission; have the CAC/unit-economics answer (AI lifts the hotel's own conversion) ready.)PB0",
    {
      R"PB0()PB0",
      R"PB0()PB0",
      R"PB0()PB0",
      R"PB0()PB0",
    },
    0,
    0,
    R"PB0(Best response principle: Tripla take rate 1.42% vs ~25% OTA; re-ground per company.)PB0",
  },
  {
    R"PB0(A04-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(A04)PB0",
    R"PB0(What confidence framing belongs with: Success / 90-day impact & successful traits?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    1,
    R"PB0(Card A04 is tagged: Framework-only — a plan, not a track record.)PB0",
  },
  {
    R"PB0(A04-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(A04)PB0",
    R"PB0(What is the main risk in a weak answer to: Success / 90-day impact & successful traits?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    4,
    R"PB0(State dated, proactive milestones; don't assume their team model; handle the legacy-mess and upstream-risk probes head-on.)PB0",
  },
  {
    R"PB0(A04-hostile)PB0",
    R"PB0(hostile_followup)PB0",
    R"PB0(A04)PB0",
    R"PB0(Interviewer pushes on 'Success / 90-day impact & successful traits': State dated, proactive milestones; don't assume their team model; handle the legacy-mess and upstream-risk probes head-on.)PB0",
    {
      R"PB0()PB0",
      R"PB0()PB0",
      R"PB0()PB0",
      R"PB0()PB0",
    },
    0,
    0,
    R"PB0(Best response principle: Forward-looking plan; the fast-POC capability is real and supports the quick-win claim.)PB0",
  },
  {
    R"PB0(A05-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(A05)PB0",
    R"PB0(What confidence framing belongs with: Why a Senior PM role / criteria for your next step?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card A05 is tagged: Evidence-backed.)PB0",
  },
  {
    R"PB0(A05-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(A05)PB0",
    R"PB0(What is the main risk in a weak answer to: Why a Senior PM role / criteria for your next step?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    0,
    R"PB0(Defuse overqualification: own a product outcome, not the org chart; say explicitly you won't reach into engineering's architecture.)PB0",
  },
  {
    R"PB0(A06-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(A06)PB0",
    R"PB0(What confidence framing belongs with: Why an MBA after a tech/research background?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card A06 is tagged: Evidence-backed.)PB0",
  },
  {
    R"PB0(A06-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(A06)PB0",
    R"PB0(What is the main risk in a weak answer to: Why an MBA after a tech/research background?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    4,
    R"PB0(Have one concrete unit-economics-killed-a-feature example ready; don't sound dismissive of research; "much of the job," not "the whole job.")PB0",
  },
  {
    R"PB0(A07-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(A07)PB0",
    R"PB0(What confidence framing belongs with: Your biggest career achievement?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card A07 is tagged: Evidence-backed.)PB0",
  },
  {
    R"PB0(A07-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(A07)PB0",
    R"PB0(What is the main risk in a weak answer to: Your biggest career achievement?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    1,
    R"PB0(Define the ROI denominator inline; foreground the transferable method; don't dwell on press for an AI role.)PB0",
  },
  {
    R"PB0(A08-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(A08)PB0",
    R"PB0(What confidence framing belongs with: Scale of startups you worked at, and where those ventures stand now?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card A08 is tagged: Evidence-backed.)PB0",
  },
  {
    R"PB0(A08-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(A08)PB0",
    R"PB0(What is the main risk in a weak answer to: Scale of startups you worked at, and where those ventures stand now?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    4,
    R"PB0(Lead with the SDTR-vs-Safed clarification; say Safed was an operating role; have CAC, 6-month stickiness, and the exact Pita transaction structure ready.)PB0",
  },
  {
    R"PB0(A09-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(A09)PB0",
    R"PB0(What confidence framing belongs with: Japanese proficiency / experience with Japanese customers?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card A09 is tagged: Evidence-backed.)PB0",
  },
  {
    R"PB0(A09-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(A09)PB0",
    R"PB0(What is the main risk in a weak answer to: Japanese proficiency / experience with Japanese customers?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    0,
    R"PB0(Demonstrate, don't disclaim; have a Japanese answer ready for the inevitable switch — including how you'd persuade conservative stakeholders to move from legacy to AI.)PB0",
  },
  {
    R"PB0(A09-hostile)PB0",
    R"PB0(hostile_followup)PB0",
    R"PB0(A09)PB0",
    R"PB0(Interviewer pushes on 'Japanese proficiency / experience with Japanese customers': Demonstrate, don't disclaim; have a Japanese answer ready for the inevitable switch — including how you'd persuade conservative stakeholders to move from legacy to AI.)PB0",
    {
      R"PB0()PB0",
      R"PB0()PB0",
      R"PB0()PB0",
      R"PB0()PB0",
    },
    0,
    0,
    R"PB0(Best response principle: ~10 yrs Tokyo; managed JP HQ in Japanese; Ginza stakeholder interviews in Japanese.)PB0",
  },
  {
    R"PB0(A10-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(A10)PB0",
    R"PB0(What confidence framing belongs with: Do you have an engineering background / justify the business+technical blend?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card A10 is tagged: Evidence-backed.)PB0",
  },
  {
    R"PB0(A10-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(A10)PB0",
    R"PB0(What is the main risk in a weak answer to: Do you have an engineering background / justify the business+technical blend?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    4,
    R"PB0(State exact PhD status; reconcile deterministic semantic-web vs stochastic LLM proactively (you bound the model with structure); drop "vibe-code.")PB0",
  },
  {
    R"PB0(A11-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(A11)PB0",
    R"PB0(What confidence framing belongs with: How you work with and manage distributed, cross-border teams?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card A11 is tagged: Evidence-backed.)PB0",
  },
  {
    R"PB0(A11-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(A11)PB0",
    R"PB0(What is the main risk in a weak answer to: How you work with and manage distributed, cross-border teams?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    2,
    R"PB0(Lead with the concrete behavior (pre-read → decision → written log); clarify Transcom was coordination, not direct management of all centers; fix the mechanism, not the symptom.)PB0",
  },
  {
    R"PB0(A12-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(A12)PB0",
    R"PB0(What confidence framing belongs with: Job-hunting status, certainty of moving, availability to join?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card A12 is tagged: Evidence-backed (situational).)PB0",
  },
  {
    R"PB0(A12-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(A12)PB0",
    R"PB0(What is the main risk in a weak answer to: Job-hunting status, certainty of moving, availability to join?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    2,
    R"PB0(Keep it clean and confident; don't name competitors or repeat framework language; stay flexible on the start-date probe.)PB0",
  },
  {
    R"PB0(A13-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(A13)PB0",
    R"PB0(What confidence framing belongs with: What you look for from the company / expectations of us?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card A13 is tagged: Evidence-backed.)PB0",
  },
  {
    R"PB0(A13-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(A13)PB0",
    R"PB0(What is the main risk in a weak answer to: What you look for from the company / expectations of us?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    4,
    R"PB0(State operational expectations, not a culture manifesto; caveat "experiment-friendly" hard for a high-stakes vertical so it doesn't sound reckless.)PB0",
  },
  {
    R"PB0(A14-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(A14)PB0",
    R"PB0(What confidence framing belongs with: Would you consider a CEO/COO role in future?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card A14 is tagged: Evidence-backed.)PB0",
  },
  {
    R"PB0(A14-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(A14)PB0",
    R"PB0(What is the main risk in a weak answer to: Would you consider a CEO/COO role in future?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    4,
    R"PB0(Reassure on flight-risk; reframe owner's mindset as prioritization discipline, not ambition for their seat; drop "survival and growth.")PB0",
  },
  {
    R"PB0(A15-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(A15)PB0",
    R"PB0(What confidence framing belongs with: Where can you help us most (e.g., within the CEO office)?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card A15 is tagged: Evidence-backed.)PB0",
  },
  {
    R"PB0(A15-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(A15)PB0",
    R"PB0(What is the main risk in a weak answer to: Where can you help us most (e.g., within the CEO office)?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    4,
    R"PB0(Never call the CEO's ideas "messy"; define "validated" concretely; frame yourself as an internal owner, not a consultant. ---)PB0",
  },
  {
    R"PB0(B16-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(B16)PB0",
    R"PB0(What confidence framing belongs with: Ideate a new product or feature for our niche?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card B16 is tagged: Evidence-backed (real ideation); TAM is an estimate.)PB0",
  },
  {
    R"PB0(B16-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(B16)PB0",
    R"PB0(What is the main risk in a weak answer to: Ideate a new product or feature for our niche?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    5,
    R"PB0(Lead with the pain + how you'd validate it; show the TAM as assumptions; have the "why us vs Copilot/CI vendor" answer ready.)PB0",
  },
  {
    R"PB0(B16-hostile)PB0",
    R"PB0(hostile_followup)PB0",
    R"PB0(B16)PB0",
    R"PB0(Interviewer pushes on 'Ideate a new product or feature for our niche': Lead with the pain + how you'd validate it; show the TAM as assumptions; have the "why us vs Copilot/CI vendor" answer ready.)PB0",
    {
      R"PB0()PB0",
      R"PB0()PB0",
      R"PB0()PB0",
      R"PB0()PB0",
    },
    0,
    0,
    R"PB0(Best response principle: Genesys Debugger (Autify work-sample ideation); TAM is your estimate. Alt: Cotomo Talk-the-News for a consumer/data niche.)PB0",
  },
  {
    R"PB0(B17-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(B17)PB0",
    R"PB0(What confidence framing belongs with: Walk through a past project — problem, contribution, hardest parts, diagnosis?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card B17 is tagged: Evidence-backed.)PB0",
  },
  {
    R"PB0(B17-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(B17)PB0",
    R"PB0(What is the main risk in a weak answer to: Walk through a past project — problem, contribution, hardest parts, diagnosis?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    1,
    R"PB0(Define retention (low-single-digit baseline; matched-cohort lift); own attribution as directional; don't muddle in Digital Senpai.)PB0",
  },
  {
    R"PB0(B17-hostile)PB0",
    R"PB0(hostile_followup)PB0",
    R"PB0(B17)PB0",
    R"PB0(Interviewer pushes on 'Walk through a past project — problem, contribution, hardest parts, diagnosis': Define retention (low-single-digit baseline; matched-cohort lift); own attribution as directional; don't muddle in Digital Senpai.)PB0",
    {
      R"PB0()PB0",
      R"PB0()PB0",
      R"PB0()PB0",
      R"PB0()PB0",
    },
    0,
    0,
    R"PB0(Best response principle: Cotomo, ~3× retention (matched cohort) + ~50% longer sessions. Alt: Digital Senpai for a B2B/RAG panel.)PB0",
  },
  {
    R"PB0(B18-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(B18)PB0",
    R"PB0(What confidence framing belongs with: Prioritize features for a roadmap or migration?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card B18 is tagged: Evidence-backed.)PB0",
  },
  {
    R"PB0(B18-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(B18)PB0",
    R"PB0(What is the main risk in a weak answer to: Prioritize features for a roadmap or migration?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    2,
    R"PB0(Lead with one concrete prioritization + when you broke the framework; don't stack RICE/ICE/I-I-O as a textbook recital.)PB0",
  },
  {
    R"PB0(B18-hostile)PB0",
    R"PB0(hostile_followup)PB0",
    R"PB0(B18)PB0",
    R"PB0(Interviewer pushes on 'Prioritize features for a roadmap or migration': Lead with one concrete prioritization + when you broke the framework; don't stack RICE/ICE/I-I-O as a textbook recital.)PB0",
    {
      R"PB0()PB0",
      R"PB0()PB0",
      R"PB0()PB0",
      R"PB0()PB0",
    },
    0,
    0,
    R"PB0(Best response principle: Safed (overrode the score on a reliability/onboarding fix; +55% engagement / −40% TTM).)PB0",
  },
  {
    R"PB0(B19-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(B19)PB0",
    R"PB0(What confidence framing belongs with: Zero-to-one discovery, and how you choose a POC / who's involved?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card B19 is tagged: Evidence-backed.)PB0",
  },
  {
    R"PB0(B19-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(B19)PB0",
    R"PB0(What is the main risk in a weak answer to: Zero-to-one discovery, and how you choose a POC / who's involved?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    5,
    R"PB0(Lead with a software example; answer the B2B-can't-pre-pay probe directly (LOIs / paid pilots / fake-door); keep Pita secondary.)PB0",
  },
  {
    R"PB0(B20-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(B20)PB0",
    R"PB0(What confidence framing belongs with: Unique value proposition vs competitors / value we bring to customers?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card B20 is tagged: Evidence-backed for Tripla.)PB0",
  },
  {
    R"PB0(B20-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(B20)PB0",
    R"PB0(What is the main risk in a weak answer to: Unique value proposition vs competitors / value we bring to customers?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    0,
    R"PB0(Don't claim live inventory is itself the moat (OTAs have PMS APIs too) — the moat is the guest relationship + switching cost; don't over-defend OTAs.)PB0",
  },
  {
    R"PB0(B21-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(B21)PB0",
    R"PB0(What confidence framing belongs with: What should we do about [industry challenge] (e.g., travelers booking via ChatGPT)?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card B21 is tagged: Evidence-backed (prototype).)PB0",
  },
  {
    R"PB0(B21-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(B21)PB0",
    R"PB0(What is the main risk in a weak answer to: What should we do about [industry challenge] (e.g., travelers booking via ChatGPT)?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    0,
    R"PB0(Correct the MCP-discovery misconception yourself; separate "machine-readable data" (real) from "agents auto-discover me" (not how MCP works).)PB0",
  },
  {
    R"PB0(B22-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(B22)PB0",
    R"PB0(What confidence framing belongs with: Why is [product area, e.g. Payments] important, and why interested given your AI background?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card B22 is tagged: Evidence-backed.)PB0",
  },
  {
    R"PB0(B22-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(B22)PB0",
    R"PB0(What is the main risk in a weak answer to: Why is [product area, e.g. Payments] important, and why interested given your AI background?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    4,
    R"PB0(Don't muddle back-office forecasting with checkout conversion; state Moneytree was consulting. ---)PB0",
  },
  {
    R"PB0(C23-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(C23)PB0",
    R"PB0(What confidence framing belongs with: How do you QA/test a non-deterministic chatbot or AI output?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card C23 is tagged: Evidence-backed (pilot-scale, not hyperscale MLOps).)PB0",
  },
  {
    R"PB0(C23-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(C23)PB0",
    R"PB0(What is the main risk in a weak answer to: How do you QA/test a non-deterministic chatbot or AI output?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    5,
    R"PB0(Lead with "much of it is deterministic" to defuse the CI-bottleneck probe; calibrate the judge against human labels; state pilot-scale maturity.)PB0",
  },
  {
    R"PB0(C23-hostile)PB0",
    R"PB0(hostile_followup)PB0",
    R"PB0(C23)PB0",
    R"PB0(Interviewer pushes on 'How do you QA/test a non-deterministic chatbot or AI output': Lead with "much of it is deterministic" to defuse the CI-bottleneck probe; calibrate the judge against human labels; state pilot-scale maturity.)PB0",
    {
      R"PB0()PB0",
      R"PB0()PB0",
      R"PB0()PB0",
      R"PB0()PB0",
    },
    0,
    0,
    R"PB0(Best response principle: Digital Senpai eval harness (designed/built at pilot scale).)PB0",
  },
  {
    R"PB0(C24-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(C24)PB0",
    R"PB0(What confidence framing belongs with: Guardrails against hallucinations / PII leaks?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card C24 is tagged: Evidence-backed on architecture; measured red-team is a framework upgrade.)PB0",
  },
  {
    R"PB0(C24-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(C24)PB0",
    R"PB0(What is the main risk in a weak answer to: Guardrails against hallucinations / PII leaks?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    4,
    R"PB0(Name OWASP LLM Top 10; lead with access control + grounding (temperature minor); fix the async hole — high-risk = synchronous pre-emission.)PB0",
  },
  {
    R"PB0(C24-hostile)PB0",
    R"PB0(hostile_followup)PB0",
    R"PB0(C24)PB0",
    R"PB0(Interviewer pushes on 'Guardrails against hallucinations / PII leaks': Name OWASP LLM Top 10; lead with access control + grounding (temperature minor); fix the async hole — high-risk = synchronous pre-emission.)PB0",
    {
      R"PB0()PB0",
      R"PB0()PB0",
      R"PB0()PB0",
      R"PB0()PB0",
    },
    0,
    0,
    R"PB0(Best response principle: Deterministic PII redaction (Japanese LM agent / Digital Senpai) — architecture is real; the red-team leak-rate is what you'd gate on.)PB0",
  },
  {
    R"PB0(C25a-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(C25a)PB0",
    R"PB0(What confidence framing belongs with: Decide an AI feature is ready to release?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card C25a is tagged: Evidence-backed on the decision; full shadow/canary mechanics are framework.)PB0",
  },
  {
    R"PB0(C25a-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(C25a)PB0",
    R"PB0(What is the main risk in a weak answer to: Decide an AI feature is ready to release?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    1,
    R"PB0(Give concrete thresholds; explain how you evaluate shadow without ground truth (vs baseline + eval set); acknowledge shadow's cost.)PB0",
  },
  {
    R"PB0(C25b-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(C25b)PB0",
    R"PB0(What confidence framing belongs with: Debug non-deterministic output?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card C25b is tagged: Evidence-backed on the harness; methodical steps are the articulation.)PB0",
  },
  {
    R"PB0(C25b-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(C25b)PB0",
    R"PB0(What is the main risk in a weak answer to: Debug non-deterministic output?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    4,
    R"PB0(Don't imply temp-0 is deterministic; lead with measuring a failure rate to separate bug from jitter; define which drift.)PB0",
  },
  {
    R"PB0(C26-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(C26)PB0",
    R"PB0(What confidence framing belongs with: AI quality measurement across multiple languages / translated output?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    1,
    R"PB0(Card C26 is tagged: Framework-only for real-time AI translation QA.)PB0",
  },
  {
    R"PB0(C26-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(C26)PB0",
    R"PB0(What is the main risk in a weak answer to: AI quality measurement across multiple languages / translated output?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    0,
    R"PB0(COMET needs references — split offline (COMET/golden sets) from online (reference-free: LLM-judge, back-translation, task-success). Don't claim COMET on live dialogue.)PB0",
  },
  {
    R"PB0(C27-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(C27)PB0",
    R"PB0(What confidence framing belongs with: Structured data for an AI agent (RAG) — what works / doesn't?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card C27 is tagged: Evidence-backed (prototype + depth); state prototype maturity.)PB0",
  },
  {
    R"PB0(C27-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(C27)PB0",
    R"PB0(What is the main risk in a weak answer to: Structured data for an AI agent (RAG) — what works / doesn't?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    5,
    R"PB0(Lead with tool-calling for live structured data (not vector-dumping); name GraphRAG as an option for relational schemas; say "prototype.")PB0",
  },
  {
    R"PB0(C28-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(C28)PB0",
    R"PB0(What confidence framing belongs with: Biggest challenges for this AI product?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card C28 is tagged: Evidence-backed.)PB0",
  },
  {
    R"PB0(C28-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(C28)PB0",
    R"PB0(What is the main risk in a weak answer to: Biggest challenges for this AI product?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    1,
    R"PB0(Give the immediate staleness mitigation (don't make it a multi-quarter platform promise); name 2–3 risks with a metric each; drop the reused conversational-agent anecdote here. ---)PB0",
  },
  {
    R"PB0(D29-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(D29)PB0",
    R"PB0(What confidence framing belongs with: Preferred style of collaborating with engineers?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card D29 is tagged: Evidence-backed.)PB0",
  },
  {
    R"PB0(D29-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(D29)PB0",
    R"PB0(What is the main risk in a weak answer to: Preferred style of collaborating with engineers?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    2,
    R"PB0(Vary the phrasing; show the "challenge trade-offs" line is technical, not positional; give the concrete intake mechanism, not "filtering noise.")PB0",
  },
  {
    R"PB0(D29-hostile)PB0",
    R"PB0(hostile_followup)PB0",
    R"PB0(D29)PB0",
    R"PB0(Interviewer pushes on 'Preferred style of collaborating with engineers': Vary the phrasing; show the "challenge trade-offs" line is technical, not positional; give the concrete intake mechanism, not "filtering noise.")PB0",
    {
      R"PB0()PB0",
      R"PB0()PB0",
      R"PB0()PB0",
      R"PB0()PB0",
    },
    0,
    0,
    R"PB0(Best response principle: Safed / 30-person Vietnam dev team; single-intake backlog.)PB0",
  },
  {
    R"PB0(D30-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(D30)PB0",
    R"PB0(What confidence framing belongs with: A time you disagreed with a technical stakeholder — resolution and outcome?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card D30 is tagged: Evidence-backed.)PB0",
  },
  {
    R"PB0(D30-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(D30)PB0",
    R"PB0(What is the main risk in a weak answer to: A time you disagreed with a technical stakeholder — resolution and outcome?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    4,
    R"PB0(Don't make the CTO look shallow or do "I told you so"; the lesson is influence-via-cheap-experiment; keep the lift cohort-scoped.)PB0",
  },
  {
    R"PB0(D30-hostile)PB0",
    R"PB0(hostile_followup)PB0",
    R"PB0(D30)PB0",
    R"PB0(Interviewer pushes on 'A time you disagreed with a technical stakeholder — resolution and outcome': Don't make the CTO look shallow or do "I told you so"; the lesson is influence-via-cheap-experiment; keep the lift cohort-scoped.)PB0",
    {
      R"PB0()PB0",
      R"PB0()PB0",
      R"PB0()PB0",
      R"PB0()PB0",
    },
    0,
    0,
    R"PB0(Best response principle: Cotomo CTO disagreement; cohort-scoped retention lift from Talk-the-News.)PB0",
  },
  {
    R"PB0(D31-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(D31)PB0",
    R"PB0(What confidence framing belongs with: Working with engineers day-to-day when products exist and there's pressure?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card D31 is tagged: Evidence-backed.)PB0",
  },
  {
    R"PB0(D31-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(D31)PB0",
    R"PB0(What is the main risk in a weak answer to: Working with engineers day-to-day when products exist and there's pressure?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    2,
    R"PB0(Translate "High-Integrity Commitments" into plain terms; pre-empt the "40% = you cut testing" probe; don't pack five frameworks in.)PB0",
  },
  {
    R"PB0(D32-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(D32)PB0",
    R"PB0(What confidence framing belongs with: Prove your proposal's value to other stakeholders / PMs?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card D32 is tagged: Evidence-backed.)PB0",
  },
  {
    R"PB0(D32-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(D32)PB0",
    R"PB0(What is the main risk in a weak answer to: Prove your proposal's value to other stakeholders / PMs?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    5,
    R"PB0(State Sparkle's real outcome (prototype + interest), not implied revenue; show you handled KYC/AML/custody as the reason for sequencing. ---)PB0",
  },
  {
    R"PB0(E33-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(E33)PB0",
    R"PB0(What confidence framing belongs with: Define a North Star metric and sub-metrics (and does it change by stage)?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card E33 is tagged: Evidence-backed (Safed real; Langets illustrative).)PB0",
  },
  {
    R"PB0(E33-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(E33)PB0",
    R"PB0(What is the main risk in a weak answer to: Define a North Star metric and sub-metrics (and does it change by stage)?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    1,
    R"PB0(Acknowledge guardrail metrics so "one indicator" isn't naive; show how you'd measure proficiency fast (proxy checkpoints) and avoid the vanity trap.)PB0",
  },
  {
    R"PB0(E33-hostile)PB0",
    R"PB0(hostile_followup)PB0",
    R"PB0(E33)PB0",
    R"PB0(Interviewer pushes on 'Define a North Star metric and sub-metrics (and does it change by stage)': Acknowledge guardrail metrics so "one indicator" isn't naive; show how you'd measure proficiency fast (proxy checkpoints) and avoid the vanity trap.)PB0",
    {
      R"PB0()PB0",
      R"PB0()PB0",
      R"PB0()PB0",
      R"PB0()PB0",
    },
    0,
    0,
    R"PB0(Best response principle: Langets (illustrative) + Safed lived North Star (active users).)PB0",
  },
  {
    R"PB0(E34-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(E34)PB0",
    R"PB0(What confidence framing belongs with: Measure the success of a newly released AI feature?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card E34 is tagged: Evidence-backed (role real).)PB0",
  },
  {
    R"PB0(E34-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(E34)PB0",
    R"PB0(What is the main risk in a weak answer to: Measure the success of a newly released AI feature?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    4,
    R"PB0(Lead with speed-only-if-quality-holds; don't say "client picks most of it = working"; mention power/interleaving.)PB0",
  },
  {
    R"PB0(E34-hostile)PB0",
    R"PB0(hostile_followup)PB0",
    R"PB0(E34)PB0",
    R"PB0(Interviewer pushes on 'Measure the success of a newly released AI feature': Lead with speed-only-if-quality-holds; don't say "client picks most of it = working"; mention power/interleaving.)PB0",
    {
      R"PB0()PB0",
      R"PB0()PB0",
      R"PB0()PB0",
      R"PB0()PB0",
    },
    0,
    0,
    R"PB0(Best response principle: VISASQ Time-to-First-Match (role real); shortlist ratio illustrative.)PB0",
  },
  {
    R"PB0(E35-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(E35)PB0",
    R"PB0(What confidence framing belongs with: Validate the correlation between a sub-metric and the North Star?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card E35 is tagged: Evidence-backed.)PB0",
  },
  {
    R"PB0(E35-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(E35)PB0",
    R"PB0(What is the main risk in a weak answer to: Validate the correlation between a sub-metric and the North Star?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    4,
    R"PB0(Name the specific confounders you controlled for; keep the lift cohort-scoped and call it directional.)PB0",
  },
  {
    R"PB0(E36-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(E36)PB0",
    R"PB0(What confidence framing belongs with: A product decision made from data analysis?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card E36 is tagged: Evidence-backed that it was a real engagement and reported outcome; treat the +40% as directional, not a personally-run RCT.)PB0",
  },
  {
    R"PB0(E36-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(E36)PB0",
    R"PB0(What is the main risk in a weak answer to: A product decision made from data analysis?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    0,
    R"PB0(State Moneytree was consulting; present +40% as directional; don't over-claim shipped ownership. ---)PB0",
  },
  {
    R"PB0(F37-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(F37)PB0",
    R"PB0(What confidence framing belongs with: Architecture and tools of your prototype, and how you built it?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card F37 is tagged: Evidence-backed (prototype).)PB0",
  },
  {
    R"PB0(F37-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(F37)PB0",
    R"PB0(What is the main risk in a weak answer to: Architecture and tools of your prototype, and how you built it?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    5,
    R"PB0(Lead with API-vs-generation separation; say "prototype, not production"; correct the MCP-discovery misconception and name the auth/scraping risks.)PB0",
  },
  {
    R"PB0(F37-hostile)PB0",
    R"PB0(hostile_followup)PB0",
    R"PB0(F37)PB0",
    R"PB0(Interviewer pushes on 'Architecture and tools of your prototype, and how you built it': Lead with API-vs-generation separation; say "prototype, not production"; correct the MCP-discovery misconception and name the auth/scraping risks.)PB0",
    {
      R"PB0()PB0",
      R"PB0()PB0",
      R"PB0()PB0",
      R"PB0()PB0",
    },
    0,
    0,
    R"PB0(Best response principle: triplaNeo (working prototype; Python/Next.js/Vercel). Alt: smartyPants hackathon build.)PB0",
  },
  {
    R"PB0(F38-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(F38)PB0",
    R"PB0(What confidence framing belongs with: Build it in terms of resources and time (POC vs production)?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card F38 is tagged: Evidence-backed (POC + production shipping).)PB0",
  },
  {
    R"PB0(F38-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(F38)PB0",
    R"PB0(What is the main risk in a weak answer to: Build it in terms of resources and time (POC vs production)?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    4,
    R"PB0(Call the 3–5× and 30/30/30 heuristics with assumptions; name the economic kill-gate (token cost vs margin).)PB0",
  },
  {
    R"PB0(F39-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(F39)PB0",
    R"PB0(What confidence framing belongs with: Resource planning: backend engineers vs ML/data specialists?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card F39 is tagged: Evidence-backed.)PB0",
  },
  {
    R"PB0(F39-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(F39)PB0",
    R"PB0(What is the main risk in a weak answer to: Resource planning: backend engineers vs ML/data specialists?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    4,
    R"PB0(Lead with "staff by uncertainty"; have the retrieval-precision-stuck reallocation example ready.)PB0",
  },
  {
    R"PB0(F40-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(F40)PB0",
    R"PB0(What confidence framing belongs with: Do you also code / how much now?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card F40 is tagged: Evidence-backed.)PB0",
  },
  {
    R"PB0(F40-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(F40)PB0",
    R"PB0(What is the main risk in a weak answer to: Do you also code / how much now?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    5,
    R"PB0(Drop "vibe-code" and "call BS"; be ready to name a real bug you debugged and the prototype's modules; don't claim production-engineer competence.)PB0",
  },
  {
    R"PB0(F41-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(F41)PB0",
    R"PB0(What confidence framing belongs with: RAG / agent-orchestration experience actually delivered?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card F41 is tagged: Evidence-backed (delivered to pilot).)PB0",
  },
  {
    R"PB0(F41-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(F41)PB0",
    R"PB0(What is the main risk in a weak answer to: RAG / agent-orchestration experience actually delivered?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    5,
    R"PB0(Define pilot scope (users/period); stress you verified accuracy held, not just speed; keep MCP out unless asked. ---)PB0",
  },
  {
    R"PB0(F41-hostile)PB0",
    R"PB0(hostile_followup)PB0",
    R"PB0(F41)PB0",
    R"PB0(Interviewer pushes on 'RAG / agent-orchestration experience actually delivered': Define pilot scope (users/period); stress you verified accuracy held, not just speed; keep MCP out unless asked. ---)PB0",
    {
      R"PB0()PB0",
      R"PB0()PB0",
      R"PB0()PB0",
      R"PB0()PB0",
    },
    0,
    0,
    R"PB0(Best response principle: Digital Senpai (delivered to pilot; ~20% / ~65%).)PB0",
  },
  {
    R"PB0(G42-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(G42)PB0",
    R"PB0(What confidence framing belongs with: Recent AI trends you're experimenting with, and how you keep sharp?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card G42 is tagged: Evidence-backed.)PB0",
  },
  {
    R"PB0(G42-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(G42)PB0",
    R"PB0(What is the main risk in a weak answer to: Recent AI trends you're experimenting with, and how you keep sharp?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    2,
    R"PB0(Lead with capabilities, not framework/protocol names; explain test-time compute as a cheap-classifier latency decision; don't re-cite the hackathon as your only proof.)PB0",
  },
  {
    R"PB0(G43-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(G43)PB0",
    R"PB0(What confidence framing belongs with: In the age of AI, how should the PM role change / qualities of a great AI PM?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    2,
    R"PB0(Card G43 is tagged: Perspective.)PB0",
  },
  {
    R"PB0(G43-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(G43)PB0",
    R"PB0(What is the main risk in a weak answer to: In the age of AI, how should the PM role change / qualities of a great AI PM?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    4,
    R"PB0(Lead with "evaluation-first" as the concrete differentiator; answer the PRD/SLA reality (error rates + guardrails, not fixed specs); keep it concrete, don't lecture.)PB0",
  },
  {
    R"PB0(G44-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(G44)PB0",
    R"PB0(What confidence framing belongs with: How you structure the AI strategy and team?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card G44 is tagged: Evidence-backed.)PB0",
  },
  {
    R"PB0(G44-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(G44)PB0",
    R"PB0(What is the main risk in a weak answer to: How you structure the AI strategy and team?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    4,
    R"PB0(Lead by solving the bottleneck (platform as self-serve API); add portfolio + cost-allocation; don't sound top-down-centralized. ---)PB0",
  },
  {
    R"PB0(H45-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(H45)PB0",
    R"PB0(What confidence framing belongs with: How long should an efficient meeting be?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    2,
    R"PB0(Card H45 is tagged: Perspective.)PB0",
  },
  {
    R"PB0(H45-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(H45)PB0",
    R"PB0(What is the main risk in a weak answer to: How long should an efficient meeting be?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    2,
    R"PB0(Don't sound abrasive about meetings; give the force-a-decision mechanism and the Japanese-exec nuance.)PB0",
  },
  {
    R"PB0(H46-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(H46)PB0",
    R"PB0(What confidence framing belongs with: What you learned in the Duke DeFi course / DeFi-Web3 motivation?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card H46 is tagged: Evidence-backed.)PB0",
  },
  {
    R"PB0(H46-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(H46)PB0",
    R"PB0(What is the main risk in a weak answer to: What you learned in the Duke DeFi course / DeFi-Web3 motivation?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    0,
    R"PB0(Don't overclaim "web3 is how agents will settle"; add the regulated-payments caveat; label it a hackathon; gate relevance to FinTech/Web3 roles. ---)PB0",
  },
  {
    R"PB0(I47-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(I47)PB0",
    R"PB0(What confidence framing belongs with: Is AI even the right tool — scoping an AI problem / AI vs a heuristic?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card I47 is tagged: Evidence-backed (real "don't over-engineer" calls).)PB0",
  },
  {
    R"PB0(I47-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(I47)PB0",
    R"PB0(What is the main risk in a weak answer to: Is AI even the right tool — scoping an AI problem / AI vs a heuristic?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    4,
    R"PB0(Drop the "Desirable/Possible/Probable" alliteration for the plain checklist; lead with the heuristic-first test; have the OCR throughput number ready.)PB0",
  },
  {
    R"PB0(I48-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(I48)PB0",
    R"PB0(What confidence framing belongs with: ML/AI system design (recommendation / search-ranking / fraud)?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card I48 is tagged: Framework-only on a hyperscale recommender; evidence-backed on the analogous matching problem.)PB0",
  },
  {
    R"PB0(I48-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(I48)PB0",
    R"PB0(What is the main risk in a weak answer to: ML/AI system design (recommendation / search-ranking / fraud)?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    4,
    R"PB0(Lead with decision-type; say two-tower is "one pattern, not universal"; treat fraud differently (graph/anomaly/rules); name cold-start; separate design knowledge from delivery.)PB0",
  },
  {
    R"PB0(I49-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(I49)PB0",
    R"PB0(What confidence framing belongs with: Foundation-model selection / build-vs-buy (cost, latency, quality)?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card I49 is tagged: Evidence-backed (the analysis is yours; the build wasn't).)PB0",
  },
  {
    R"PB0(I49-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(I49)PB0",
    R"PB0(What is the main risk in a weak answer to: Foundation-model selection / build-vs-buy (cost, latency, quality)?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    4,
    R"PB0(Say "build" usually = fine-tune/distill/self-host; give reasons beyond moat (privacy/latency/cost/control); state your advisory role on the from-scratch build; mention model portfolio/routing.)PB0",
  },
  {
    R"PB0(I50-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(I50)PB0",
    R"PB0(What confidence framing belongs with: Prompt engineering vs RAG vs fine-tuning?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card I50 is tagged: Evidence-backed.)PB0",
  },
  {
    R"PB0(I50-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(I50)PB0",
    R"PB0(What is the main risk in a weak answer to: Prompt engineering vs RAG vs fine-tuning?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    4,
    R"PB0(Reframe as knowledge (RAG/tools) vs behavior/format (fine-tune) vs workflow (agents); note LoRA/many-shot; add freshness handling.)PB0",
  },
  {
    R"PB0(I51-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(I51)PB0",
    R"PB0(What confidence framing belongs with: Responsible AI, privacy, and compliance for B2B in Japan?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card I51 is tagged: Evidence-backed on operating-under-constraint + privacy-by-design; APPI/governance specifics are framework-led.)PB0",
  },
  {
    R"PB0(I51-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(I51)PB0",
    R"PB0(What is the main risk in a weak answer to: Responsible AI, privacy, and compliance for B2B in Japan?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    1,
    R"PB0(Name NIST AI RMF + OWASP LLM Top 10; don't conflate KYC/AML with AI governance; give PII specifics (NER+policy) and the vector-store deletion problem; explainability = attribution/logs, not SHAP.)PB0",
  },
  {
    R"PB0(I52-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(I52)PB0",
    R"PB0(What confidence framing belongs with: Human-in-the-loop / graduated autonomy?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card I52 is tagged: Evidence-backed.)PB0",
  },
  {
    R"PB0(I52-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(I52)PB0",
    R"PB0(What is the main risk in a weak answer to: Human-in-the-loop / graduated autonomy?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    4,
    R"PB0(Gate autonomy by reversibility/blast-radius, not just acceptance %; call it an improvement/eval-data queue (not "retraining" for RAG); name reviewer-bias/label-noise risk.)PB0",
  },
  {
    R"PB0(I53-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(I53)PB0",
    R"PB0(What confidence framing belongs with: Latency & cost optimization?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card I53 is tagged: Evidence-backed on the trade-off; router/caching/infra are framework you'd apply, with deep infra owned by engineering.)PB0",
  },
  {
    R"PB0(I53-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(I53)PB0",
    R"PB0(What is the main risk in a weak answer to: Latency & cost optimization?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    0,
    R"PB0(Lead with budget-as-gate; caveat cache invalidation and router latency; don't claim to hand-tune compression/speculative decoding — frame as awareness + lean on engineering.)PB0",
  },
  {
    R"PB0(I54-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(I54)PB0",
    R"PB0(What confidence framing belongs with: Agentic system design?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card I54 is tagged: Evidence-backed.)PB0",
  },
  {
    R"PB0(I54-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(I54)PB0",
    R"PB0(What is the main risk in a weak answer to: Agentic system design?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    4,
    R"PB0(Replace "reflection" with verification + state machine + allowlists + rollback; note single-orchestrator is often safer; connect contract-net/blackboard to modern orchestration to turn the "dated PhD" probe into a strength.)PB0",
  },
  {
    R"PB0(I55-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(I55)PB0",
    R"PB0(What confidence framing belongs with: AI business case / ROI?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card I55 is tagged: Evidence-backed.)PB0",
  },
  {
    R"PB0(I55-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(I55)PB0",
    R"PB0(What is the main risk in a weak answer to: AI business case / ROI?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    4,
    R"PB0(Lead with unit economics (value-per-task − full cost-to-serve incl. data toil); anchor on Safed P&L, not the Pita watch ROI; name a concrete equation.)PB0",
  },
  {
    R"PB0(I56-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(I56)PB0",
    R"PB0(What confidence framing belongs with: Data strategy for an AI product?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    0,
    R"PB0(Card I56 is tagged: Evidence-backed.)PB0",
  },
  {
    R"PB0(I56-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(I56)PB0",
    R"PB0(What is the main risk in a weak answer to: Data strategy for an AI product?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    4,
    R"PB0(Qualify the flywheel with feedback bias (selection/position/survivorship/model-induced); add data contracts; call it an improvement/eval-data queue. ---)PB0",
  },
  {
    R"PB0(J57-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(J57)PB0",
    R"PB0(What confidence framing belongs with: Incident response for an AI failure?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    3,
    R"PB0(Card J57 is tagged: Framework-led with a real severity-weighted-rollback anchor (no public AI incident run).)PB0",
  },
  {
    R"PB0(J57-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(J57)PB0",
    R"PB0(What is the main risk in a weak answer to: Incident response for an AI failure?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    4,
    R"PB0(Lead with classify→contain→remediate→RCA→prevent; name the fallback and the eval-set update; don't promise zero incidents.)PB0",
  },
  {
    R"PB0(J58-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(J58)PB0",
    R"PB0(What confidence framing belongs with: Enterprise AI procurement / buyer trust?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    3,
    R"PB0(Card J58 is tagged: Framework-led, grounded in real regulated-B2B / Japan experience (no formal SOC2/ISO program run).)PB0",
  },
  {
    R"PB0(J58-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(J58)PB0",
    R"PB0(What is the main risk in a weak answer to: Enterprise AI procurement / buyer trust?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    0,
    R"PB0(Frame trust as a product deliverable (questionnaire, retention, audit logs, override, eval report); offer bounded liability + incident process, not an uncapped guarantee.)PB0",
  },
  {
    R"PB0(J59-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(J59)PB0",
    R"PB0(What confidence framing belongs with: Model/provider operational risk, portability & Day-2 drift?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    3,
    R"PB0(Card J59 is tagged: Framework-led (sound design; not operated at scale).)PB0",
  },
  {
    R"PB0(J59-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(J59)PB0",
    R"PB0(What is the main risk in a weak answer to: Model/provider operational risk, portability & Day-2 drift?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    4,
    R"PB0(Lead with abstraction + cross-provider eval + fallback; name the silent-weight-change → re-calibrate-evals trap; review retention/training-use terms.)PB0",
  },
  {
    R"PB0(J60-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(J60)PB0",
    R"PB0(What confidence framing belongs with: Data rights & consent — using customer data for training and evaluation?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    3,
    R"PB0(Card J60 is tagged: Framework-led, grounded in real PII / Japan-B2B experience.)PB0",
  },
  {
    R"PB0(J60-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(J60)PB0",
    R"PB0(What is the main risk in a weak answer to: Data rights & consent — using customer data for training and evaluation?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    4,
    R"PB0(Lead with "logs ≠ training/eval data; needs consent/contract, per-tenant in Japan"; name anonymization, opt-out, retention, prod/eval separation, synthetic fallback.)PB0",
  },
  {
    R"PB0(J61-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(J61)PB0",
    R"PB0(What confidence framing belongs with: Pricing & packaging for an AI feature?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    3,
    R"PB0(Card J61 is tagged: Framework-led, grounded in real commercial/P&L experience (no specific AI-pricing case claimed).)PB0",
  },
  {
    R"PB0(J61-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(J61)PB0",
    R"PB0(What is the main risk in a weak answer to: Pricing & packaging for an AI feature?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    1,
    R"PB0(Start from value metric + cost-to-serve; weigh usage vs seat vs outcome with the predictability/margin trade-offs; protect margin with caps; paid pilot over free beta.)PB0",
  },
  {
    R"PB0(J62-confidence)PB0",
    R"PB0(mcq)PB0",
    R"PB0(J62)PB0",
    R"PB0(What confidence framing belongs with: Legacy-infrastructure migration & corporate gridlock (Japan enterprise)?)PB0",
    {
      R"PB0(Evidence-backed)PB0",
      R"PB0(Framework-only)PB0",
      R"PB0(Perspective)PB0",
      R"PB0(Unclear)PB0",
    },
    4,
    3,
    R"PB0(Card J62 is tagged: Framework-led, grounded in real Japan-enterprise / incremental-delivery experience.)PB0",
  },
  {
    R"PB0(J62-watch)PB0",
    R"PB0(weak_answer)PB0",
    R"PB0(J62)PB0",
    R"PB0(What is the main risk in a weak answer to: Legacy-infrastructure migration & corporate gridlock (Japan enterprise)?)PB0",
    {
      R"PB0(overclaims maturity)PB0",
      R"PB0(weak metric framing)PB0",
      R"PB0(too generic)PB0",
      R"PB0(sounds defensive)PB0",
    },
    4,
    1,
    R"PB0(Lead with strangler-pattern + access-layer (not big-bang); name the org gate (data owners / legacy-team relationship) as the real first-30-days work; sequence by value × access-friction. --- # PART 2 — The questions you ask them Pick 3–5 and lead with #1. Phrased so you can say them close to verbatim. 1. "Six months in, what would make you say this hire was clearly the right one?" — Lead with this. Surfaces the real success criteria and reads senior. 2. "What's the biggest struggle right now — generating ideas, building POCs, or shipping to production?" — Pinpoints where you add value. Best with CEO/founder or hiring manager. 3. "Where does AI sit in the strategy — a core platform or a feature — and which in-house AI bets are you most excited about?" — Tells you whether AI here is real or decorative. Best with leadership. 4. "How do customer or operator insights reach the roadmap today — what's the path from a real pain to a prioritized build?" — Probes discovery rigor without lecturing. Best with a product leader. 5. "What's your vision for the next one, three, and five years, and where's the market expansion?" — Long-horizon interest. Good with founders. 6. "In the first quarter, what would I own outright versus influence?" — Clarifies real scope and autonomy. 7. "How are decisions made when AI work is uncertain — who calls it, and how do you avoid over-investing in unproven bets?" — Shows you've shipped AI. Best with a CTO/technical panel. 8. (Late / logistics) "Does the company support AI tooling, learning, and conference attendance?" — Fine as a closing question. Ask carefully or skip. "Why did [predecessor] leave?" / "Why have so many people left recently?" read as red-flag-hunting. Soften to "How has the team evolved over the last year?" Selection by room. CEO/founder → #1 + #2 + #5. Product leader → #1 + #4 + #6. CTO/technical panel → #1 + #3 + #7. --- End of prep sheet (v2, hardened). Coverage: Part 1 categories A–J (63 cards: A–H = 46 with item 25 split = 47; Section I = 47–56; Section J = 57–62) + Part 2 (8 questions to ask). All metrics locked and defensibly framed; maturity stated in each answer; standards named only where defensible.)PB0",
  },
  {
    R"PB0(metric-01)PB0",
    R"PB0(metric_precision)PB0",
    R"PB0()PB0",
    R"PB0(Which phrasing is most defensible for ~3× retention (Cotomo)?)PB0",
    {
      R"PB0(low-single-digit baseline; matched cohort; fixed period; my action = the Talk-the-News loop; directional, not an RCT)PB0",
      R"PB0(Claim it as a clean randomized causal result.)PB0",
      R"PB0(Use the largest number without naming denominator.)PB0",
      R"PB0(Avoid baseline and cohort details.)PB0",
    },
    4,
    0,
    R"PB0(low-single-digit baseline; matched cohort; fixed period; my action = the Talk-the-News loop; directional, not an RCT)PB0",
  },
  {
    R"PB0(metric-02)PB0",
    R"PB0(metric_precision)PB0",
    R"PB0()PB0",
    R"PB0(Which phrasing is most defensible for ~50% longer sessions?)PB0",
    {
      R"PB0(session = a usage session; target cohort; directional)PB0",
      R"PB0(Claim it as a clean randomized causal result.)PB0",
      R"PB0(Use the largest number without naming denominator.)PB0",
      R"PB0(Avoid baseline and cohort details.)PB0",
    },
    4,
    0,
    R"PB0(session = a usage session; target cohort; directional)PB0",
  },
  {
    R"PB0(metric-03)PB0",
    R"PB0(metric_precision)PB0",
    R"PB0()PB0",
    R"PB0(Which phrasing is most defensible for 2.5× user growth (Safed)?)PB0",
    {
      R"PB0(full-time operating role, not advisory; have CAC + 6-month stickiness ready)PB0",
      R"PB0(Claim it as a clean randomized causal result.)PB0",
      R"PB0(Use the largest number without naming denominator.)PB0",
      R"PB0(Avoid baseline and cohort details.)PB0",
    },
    4,
    0,
    R"PB0(full-time operating role, not advisory; have CAC + 6-month stickiness ready)PB0",
  },
  {
    R"PB0(metric-04)PB0",
    R"PB0(metric_precision)PB0",
    R"PB0()PB0",
    R"PB0(Which phrasing is most defensible for 250% ROI (Pita)?)PB0",
    {
      R"PB0(on the initial investment; year-two basis)PB0",
      R"PB0(Claim it as a clean randomized causal result.)PB0",
      R"PB0(Use the largest number without naming denominator.)PB0",
      R"PB0(Avoid baseline and cohort details.)PB0",
    },
    4,
    0,
    R"PB0(on the initial investment; year-two basis)PB0",
  },
  {
    R"PB0(metric-05)PB0",
    R"PB0(metric_precision)PB0",
    R"PB0()PB0",
    R"PB0(Which phrasing is most defensible for +40% retention (Moneytree)?)PB0",
    {
      R"PB0(consulting engagement; targeted segment; directional)PB0",
      R"PB0(Claim it as a clean randomized causal result.)PB0",
      R"PB0(Use the largest number without naming denominator.)PB0",
      R"PB0(Avoid baseline and cohort details.)PB0",
    },
    4,
    0,
    R"PB0(consulting engagement; targeted segment; directional)PB0",
  },
  {
    R"PB0(metric-06)PB0",
    R"PB0(metric_precision)PB0",
    R"PB0()PB0",
    R"PB0(Which phrasing is most defensible for ~20% fewer queries / ~65% faster (Digital Senpai)?)PB0",
    {
      R"PB0(pilot scope (bounded users, fixed period); verified accuracy held, not just speed)PB0",
      R"PB0(Claim it as a clean randomized causal result.)PB0",
      R"PB0(Use the largest number without naming denominator.)PB0",
      R"PB0(Avoid baseline and cohort details.)PB0",
    },
    4,
    0,
    R"PB0(pilot scope (bounded users, fixed period); verified accuracy held, not just speed)PB0",
  },
  {
    R"PB0(metric-07)PB0",
    R"PB0(metric_precision)PB0",
    R"PB0()PB0",
    R"PB0(Which phrasing is most defensible for −40% time-to-market (Safed)?)PB0",
    {
      R"PB0(baseline cycle time; came from cutting rework and decision latency, not cutting QA)PB0",
      R"PB0(Claim it as a clean randomized causal result.)PB0",
      R"PB0(Use the largest number without naming denominator.)PB0",
      R"PB0(Avoid baseline and cohort details.)PB0",
    },
    4,
    0,
    R"PB0(baseline cycle time; came from cutting rework and decision latency, not cutting QA)PB0",
  },
};

}  // namespace embedded_papercoach
